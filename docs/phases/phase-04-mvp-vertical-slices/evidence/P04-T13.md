# Evidencia P04-T13 — Suite integrada del MVP

**Estado:** accepted  
**Implementación:** `4fbe05b`, `3bf6275`, `e948cb2`, `095be85` merged `562d29b`  
**Rojo observado (original):** `ModuleNotFoundError: No module named 'tests.fixtures.mvp'`  
**Revisión:** conformidad PASS; calidad PASS (PR #23)

## Resultado

Journeys que **componen APIs públicas** (dos tenants, clock inyectado, sin sleep real):

- FAQ A/B por canal simulado.
- create → reschedule (workflows reales) + `ReminderScheduler.upsert` **desde el test**.
- cancel replay con `CountingCapability` (segunda mutación fallaría).
- `HandoffService.create(explicit_request)` **desde el test**.
- timeout de create → `manual_review_required` (workflow real); handoff `persistent_error` **compuesto a mano**, no auto-escalado.
- restart SQL del workflow; job de reminder retomado tras upsert de test.
- prompt spoofing: `tenant_tools` con tools de appointments; `tool_schemas` / `tool_names` vacíos.

**No** se cableó `ReminderScheduler.upsert` en create/reschedule ni `HandoffService` desde workflows (fuera de scope; decisión del coordinador).

## Drift del test-plan

`docs/phases/phase-04-mvp-vertical-slices/test-plan.md` nombra `tests/e2e/test_appointments.py`. Ese archivo **no existe** (docs-owner). Las suites viven en `test_appointment_create.py`, `test_appointment_cancel.py`, `test_appointment_lifecycle.py`. No se reescribió el test-plan. El shim de re-import se eliminó para no duplicar colección.

`pytest -m e2e tests/e2e --collect-only` → 23 tests, **ninguno duplicado**. Marca `e2e` explícita en los tres tests de `test_handoff.py`. Sin `pytest_collection_modifyitems` blanket.

## Verificación (tras review)

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/e2e/test_mvp_journeys.py tests/resilience/test_mvp_failures.py -v
9 passed, 1 warning in 1.66s

uv run --extra quality pytest -m e2e tests/e2e --collect-only
23 tests collected (0 duplicates)

uv run --extra quality ruff check tests/e2e/conftest.py tests/e2e/test_mvp_journeys.py tests/e2e/test_handoff.py tests/resilience/test_mvp_failures.py tests/fixtures/mvp.py
All checks passed!
```

Warning residual: `StarletteDeprecationWarning` httpx/httpx2 (ajeno).

## Inherited vs path integrado

| Recorrido | Qué es real en T13 | Qué es composición de test |
|---|---|---|
| FAQ A/B | `POST /v1/simulated/messages` | — |
| create/reschedule | workflows + FakeAppointmentCapability | `ReminderScheduler.upsert` / `ConfirmationIngress` no salen de create/reschedule/canal |
| cancel replay | CancelAppointmentDefinition + counting cancel==1 | — |
| handoff explícito | HandoffService.create + human_owned | no hay harness→HandoffService por texto del usuario |
| timeout create | estado `manual_review_required` | no hay auto-handoff `persistent_error` |
| restart | workflow SQL reanuda | job requiere upsert de test (create no emite evento) |
| spoofing | compiler+harness con tenant_tools de appointments; HTTP extra `tenant_id` 422 | — |

## Criterios

Leyenda de estado: `satisfied` (observado en path T13 o heredado de slice aceptada **sin** reclamar el hook de producción faltante); `blocked` (falta adapter/hook de dominio; T13 no puede parchearlo).

| ID | Heredado (slice) | Path integrado T13 | Estado |
|---|---|---|---|
| AC-P04-001 | T05 FAQ e2e | `test_faq_tenants_a_and_b_use_own_corpus` | satisfied |
| AC-P04-002 | T05 `test_duplicate_external_message_reuses_run_and_outbox` | no re-ejecutado como journey propio | satisfied (heredado T05) |
| AC-P04-003 | T01 `test_agent_run_captures_config_version_and_correlation_id` | — | satisfied (heredado T01) |
| AC-P04-004 | T04 compiler/harness disabled skill | — | satisfied (heredado) |
| AC-P04-005 | T04 `test_compiler_omits_full_config_and_only_keeps_pertinent_policy` | — | satisfied (heredado) |
| AC-P04-006 | mismo | — | satisfied (heredado) |
| AC-P04-007 | T01/security isolation | — | satisfied (heredado) |
| AC-P04-008 | T01 runtime audit | — | satisfied (heredado) |
| AC-P04-009 | T03/T05 ingest+publish | faq_stack | satisfied |
| AC-P04-010 | T03 draft/failed excluded | — | satisfied (heredado) |
| AC-P04-011 | security `test_search_under_a_never_returns_tenant_b_canary_chunk` | — | satisfied (heredado) |
| AC-P04-012 | T04 FAQ source IDs | — | satisfied (heredado) |
| AC-P04-013 | T04 insufficient | — | satisfied (heredado) |
| AC-P04-014 | T04/security PDF injection | `test_prompt_spoofing_...` con `tenant_tools` appointments; `tool_schemas==()` y `tool_names==()` | satisfied |
| AC-P04-015 | T05 simulated endpoint | FAQ journey 202+outbox | satisfied |
| AC-P04-016 | — | `TenantService`/`capture` no leen `tenant.status=suspended`. Causa: falta gate de tenancy/configuración. | **blocked** |
| AC-P04-017 | T04 retrieval down | — | satisfied (heredado) |
| AC-P04-018 | capture de `config_version` en T01 no publica una versión nueva **durante** un run | T13 no tiene test que haga publish+activate mid-run | **blocked** |
| AC-P04-019 | T05 | FAQ A/B journey | satisfied |
| AC-P04-020 | T08 create workflow | journey create completed | satisfied |
| AC-P04-021 | T07 required_fields | — | satisfied (heredado) |
| AC-P04-022 | T07 invalid fields | — | satisfied (heredado) |
| AC-P04-023 | T07 search MCP | — | satisfied (heredado) |
| AC-P04-024 | T07 slots sin token | — | satisfied (heredado) |
| AC-P04-025 | T08 revalidate | — | satisfied (heredado) |
| AC-P04-026 | T08 create replay | — | satisfied (heredado) |
| AC-P04-027 | T08 timeout → manual review | `test_create_timeout_requires_manual_review` | satisfied (solo el half de review; ver auto-handoff abajo) |
| AC-P04-030 | T09 cancel confirma | — | satisfied (heredado) |
| AC-P04-031 | T09 cancel replay | journey + `operations.count("cancel")==1` | satisfied |
| AC-P04-032 | T09 already cancelled | — | satisfied (heredado) |
| AC-P04-033 | T10 reschedule | journey reschedule completed | satisfied |
| AC-P04-034 | T10 lost slot | — | satisfied (heredado) |
| AC-P04-035 | T10 timeout → review | — | satisfied (heredado) |
| AC-P04-036 | T10 confirm once | confirm del journey usa `ConfirmationIngress` de test, no canal | satisfied (heredado T10); path reminder→canal **no** observado |
| AC-P04-037 | T10 already confirmed | — | satisfied (heredado) |
| AC-P04-038 | T10 cross-tenant | get B del appointment A falla en journey | satisfied |
| AC-P04-040 | T11 `test_explicit_request_reaches_operator_fake` (`HandoffService.create`) | composición igual; **no** hay auto-handoff desde harness/canal | satisfied (heredado T11 API); integrado auto-escalado **blocked** (falta harness→HandoffService) |
| AC-P04-041 | T11 sanitize | journey explicit sanitiza password | satisfied (heredado + composición API) |
| AC-P04-042 | T11 transacción | — | satisfied (heredado) |
| AC-P04-043 | T11 replay | — | satisfied (heredado) |
| AC-P04-044 | T11 human_owned guard | journey tras create de test | satisfied (guard real; el create de handoff es de test) |
| AC-P04-045 | T11 provider down | — | satisfied (heredado) |
| AC-P04-046 | T11 isolation | inbox B vacío en journey | satisfied |
| AC-P04-050 | T12 `test_upsert_schedules_48h_before_in_buenos_aires_timezone` / SQL | create **no** emite evento; journey hace upsert de test | **blocked** en path integrado (falta create→`ReminderScheduler.upsert`); **inherited from P04-T12** para el cálculo 48h del scheduler |
| AC-P04-051 | T12 policy lead_hours | — | satisfied (heredado T12) |
| AC-P04-052 | T12 skip confirmed/cancelled | — | satisfied (heredado T12) |
| AC-P04-053 | T12 replay outbox | — | satisfied (heredado T12) |
| AC-P04-054 | T12 worker restart | T13 retoma job tras upsert de test + clock `DUE_AT+1s`; workflow SQL restart es real | satisfied (job: composición; workflow: integrado) |
| AC-P04-055 | T12 `test_reminder_then_yes_reply_confirms_appointment` (upsert+ingress) | T13 llama `ConfirmationIngress.apply_reply` desde el test; no hay channel-ingress | **blocked** en path integrado (falta canal→`ConfirmationIngress`); **inherited from P04-T12** para ingress tras reminder dispatched |
| AC-P04-056 | T12 channel failure audit | T13 composición upsert+fail_forever | satisfied (heredado T12; T13 es composición) |
| AC-P04-057 | T12 tenant isolation jobs | journey no usa canal B | satisfied (heredado T12 + composición aislada) |
| AC-P04-058 | T12 `test_reupsert_replaces_schedule_and_increments_version` | reschedule **no** llama upsert; journey re-upsert de test `schedule_version==2` | **blocked** en path integrado (falta reschedule→`ReminderScheduler.upsert`); **inherited from P04-T12** para reemplazo de versión |

Auto-escalado post-timeout (no es AC-P04-027): falta workflow/harness → `HandoffService.create(persistent_error)`. `test_handoff_composed_after_timeout_does_not_auto_escalate` documenta la composición y **no** la reclama como auto-handoff.

## Revisión y riesgos

- Two-stage review: conformidad PASS; calidad PASS. Critical/Important abiertos: ninguno.
- I2: eliminado `test_appointments.py`; marcas e2e explícitas en handoff; sin blanket collection hook.
- I3: spoofing siembra `APPOINTMENT_TOOLS` en `tenant_tools` y afirma `tool_schemas` / `tool_names` vacíos (puede fallar si el compiler/harness filtra mal).
- Cancel replay: `CountingCapability.operations.count("cancel")==1`.
- Restart clock: `DUE_AT + timedelta(seconds=1)`, no después de `starts_at`.
- Residual de dominio (no parcheado): create/reschedule/cancel no emiten schedule events; harness no crea handoff; `tenant.status=suspended` no está gateado; publish mid-run no cubierto.
- Próxima tarea desbloqueada: P06-T03, P06-T04, P07-T01, P07-T02, P08-T01 (P06-T01 ya ready).
