# Evidencia P09-T03 — Generic MCP SSE client + discovery

**Estado:** in_review  
**Implementación:** `implementation/P09-T03-mcp-client`  
**Rojo observado (inicial):** `ModuleNotFoundError: No module named 'ia_mcp.mcp.discovery'`  
**Rojo observado (review):** `ImportError: cannot import name 'DiscoveredToolCatalog'`  
**Calidad:** `auth_reference` no viaja en headers, logs ni `safe_message`. `list_tools` falla con `DomainError` público.

## Resultado

`SseMcpClient` consume `McpTarget` (`server_id`, `endpoint`, `auth_reference`, `allowed_tools`). `list_tools` → `DiscoveredToolCatalog` (intersección con `allowed_tools`). `call_tool` → `ToolResult`. `HostAllowlist` fail-closed se aplica antes de cualquier I/O: `https` si el host está listado; `http` solo con el par `http://host`. Sesiones por tenant; RPC concurrente ruteado por id. Audit de invoke queda en P09-T04.

## Archivos

- `src/ia_mcp/mcp/discovery.py`
- `src/ia_mcp/mcp/client.py`
- `src/ia_mcp/mcp/executor.py` — solo `HostAllowlist.permits` (http explícito). `_dispatch` / appointments sin cambios.
- `tests/unit/mcp/test_discovery.py`
- `tests/unit/mcp/test_client.py` _(renombrado desde `test_sse_client.py`)_
- `tests/unit/mcp/fakes/sse_server.py`
- `tests/e2e/test_mcp_discovery.py` _(skip sin `MCP_SSE_URL`)_
- `docs/phases/phase-09-mcp-discovery/evidence/P09-T03.md`

## TDD

- RED: módulos ausentes / `DiscoveredToolCatalog` ausente.
- GREEN: fake in-process `127.0.0.1`; discovery + invoke; allowlist; concurrencia; JSON-RPC `error` / `isError`; `DomainError` en `list_tools`.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/unit/mcp/test_discovery.py tests/unit/mcp/test_client.py -v
16 passed in 7.11s

uv run --extra quality ruff check src/ia_mcp/mcp/discovery.py src/ia_mcp/mcp/client.py src/ia_mcp/mcp/executor.py tests/unit/mcp/test_discovery.py tests/unit/mcp/test_client.py tests/unit/mcp/fakes/sse_server.py tests/e2e/test_mcp_discovery.py
All checks passed!

uv run --extra quality mypy src/ia_mcp/mcp/discovery.py src/ia_mcp/mcp/client.py src/ia_mcp/mcp/executor.py
Success: no issues found in 3 source files

uv run --extra quality pytest tests/security/test_prompt_injection.py::test_endpoint_outside_host_allowlist_is_rejected_before_capability -v
1 passed
```

CI no requiere `192.168.1.247`. E2E live solo si `MCP_SSE_URL` está definido.

## Criterios

| Criterio | Prueba | Resultado |
|---|---|---|
| AC-P09-005 discovery | `test_list_tools_discovers_crear_turno_and_buscar_eventos`, `test_list_tools_intersects_allowed_tools` | pass |
| AC-P09-006 invoke | `test_call_tool_happy_path_returns_tool_result`, `test_jsonrpc_error_object_is_not_ok`, `test_tool_is_error_result_is_not_ok` | pass |
| AC-P09-012 allowlist | `test_host_not_allowlisted_makes_no_request`, `test_http_loopback_works_when_host_is_listed` | pass |
| TenantContext | `test_public_methods_require_tenant_context` | pass |
| Concurrencia | `test_concurrent_calls_on_same_session_both_succeed` | pass |
| Error público | `test_list_tools_failure_is_public_domain_error` | pass |

## Decisiones y desviaciones

Se consume `McpTarget` de `executor.py`. `HostAllowlist.permits` acepta `http` solo como par `http://host` para no relajar el test de plaintext sobre hosts https-allowlisted. No se escribió ADR. `registry.py` y `_dispatch` no se tocaron. Audit de invoke: T04.

## Riesgos residuales

Handshake `2024-11-05`. `discovery.py` importa `McpTarget` desde `executor.py`; T04 debe importar el cliente después de definir `McpTarget` para evitar ciclo.

## Handoff

**Próxima tarea desbloqueada:** P09-T04  
**Revisión requerida:** conformidad y calidad
