# Evidencia P09-T04 — Generic executor dispatch

**Tarea:** P09-T04 — Generic executor dispatch  
**Estado:** in_review  
**Rama:** `implementation/P09-T04-generic-executor`  
**Base:** `f401e23` (`origin/main`, P09-T01–T03 accepted)

## Resultado

Tras `authorize` + `McpResolver` + `HostAllowlist` (fail-closed; `http` solo si `http://host` está listado):

- nombre canónico `appointments.*` (`KNOWN_TOOLS`) con `AppointmentCapability` cableada → dispatch existente (workflows P03 intactos);
- tool autorizada no canónica (p. ej. `crear_turno`) → `McpTransportClient.call_tool(tenant, target, name, arguments)`;
- host no allowlisted o tool fuera de la intersección → `ForbiddenTool` sin invocar transporte ni capability.

`McpTransportClient` es un Protocol inyectado (fake en tests; `SseMcpClient` compatible sin import en `executor.py`, sin ciclo). Audit en ambos caminos. `TenantContext` en llamadas tenant-scoped. Sin REST médico, sin ramas por slug, sin secretos en audit.

## TDD

- RED: `TypeError: ToolExecutor.__init__() got an unexpected keyword argument 'transport'`
- GREEN: constructor `transport=`; `_dispatch` usa capability si `call.name in KNOWN_TOOLS`, si no `transport.call_tool` cuando hay target.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/unit/mcp/test_executor.py tests/security/test_tool_contracts.py -v
# RED: 6 failed (transport kwarg ausente), 10 passed
# GREEN: 16 passed in 0.19s

uv run --extra quality mypy src/ia_mcp/mcp/executor.py
Success: no issues found in 1 source file

uv run --extra quality ruff check src/ia_mcp/mcp/executor.py tests/unit/mcp/test_executor.py tests/security/test_tool_contracts.py
All checks passed!
```

## Archivos

- `src/ia_mcp/mcp/executor.py`
- `tests/unit/mcp/test_executor.py`
- `tests/security/test_tool_contracts.py`
- `docs/phases/phase-09-mcp-discovery/evidence/P09-T04.md`

## Criterios

| Criterio | Prueba | Resultado |
|---|---|---|
| AC-P09-007 capability canónica | `test_canonical_appointments_search_uses_capability_when_wired` | pass |
| AC-P09-008 invoke genérico | `test_authorized_non_canonical_tool_calls_generic_client_not_capability` | pass |
| AC-P09-012 allowlist + audit + TenantContext | `test_host_not_allowlisted_is_forbidden_without_calling_client`, `test_tool_outside_intersection_is_forbidden_without_calling_client`, audit sin secretos en path genérico | pass |
| Security fail-closed | `test_host_not_allowlisted_never_invokes_transport`, `test_tool_outside_intersection_never_invokes_transport` | pass |

## Decisiones

Ninguna ADR nueva. Se inyecta `McpTransportClient` por constructor; no se importa `SseMcpClient` desde `executor.py`.

## Desviaciones

Ninguna. `discovery.py` / `client.py` y firmas públicas de workflows no se tocaron. `HostAllowlist.permits` no se relajó.

## Riesgos residuales

Sin composition root en `src/` que construya `ToolExecutor`; el cableado de `SseMcpClient` queda en el caller (tests/fixtures). Tool canónica sin capability no es un caso construible (`capability` sigue requerido). Residual no bloqueante: timeout vive en el cliente T03, no en el executor.

## Próximo paso

Revisión de dos etapas. P09-T05+ según tablero. P05-T01–T04 siguen `blocked` por EXT.
