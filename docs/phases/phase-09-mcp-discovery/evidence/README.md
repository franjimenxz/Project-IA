# Evidencia — Fase 9

Guardar:

- salida de tests de intersección y discovery (fake in-process);
- caso negativo host no allowlisted;
- regression de dispatch `appointments.*`;
- diff de registry/compiler/validators;
- opcional: captura de E2E con `MCP_SSE_URL` (sin secret values).

No commitear credenciales, tokens ni payloads con PII.
