# Evidencia P09-T02 — Open tool registry (discoverable names)

**Estado:** in_review  
**Implementación:** `implementation/P09-T02-open-registry`  
**Owner:** Core acepta los nombres que un MCP anuncia. La autorización es solo intersección de tres allowlists. `KNOWN_TOOLS` sigue exportado como set canónico de appointments; no es un mundo cerrado.

## Resultado

`available(server, tenant, skill)` es `frozenset` intersection. Un nombre presente en las tres listas se autoriza aunque no esté en `KNOWN_TOOLS` (p. ej. `crear_turno`, `appointments.delete`). Falta en cualquiera de las tres → `ForbiddenTool`.

Discovery no queda como letra muerta:

- `ContextCompiler` acepta `server_tools` inyectado; si falta, usa `tenant_tools`. Ya no pasa `server=KNOWN_TOOLS`.
- El skill appointments usa `TenantConfig.enabled_tools` si está presente; si no, no recorta a los seis nombres canónicos.
- FAQ y handoff siguen con set vacío.
- Evals y onboarding dejan de rechazar un nombre solo por estar fuera de `KNOWN_TOOLS`. Siguen fallando closed: overlap allowed∩forbidden, tool de skill deshabilitado, allowlists vacíos de FAQ.

`TenantContext` sigue obligatorio en boundaries tenant-scoped. No hay slugs institucionales en Core ni secretos en pruebas/logs.

## TDD

- RED: `pytest tests/unit/mcp/test_registry.py::test_discovered_tool_in_intersection_is_allowed -v` → `crear_turno` ausente del resultado porque `available` filtraba `if name in KNOWN_TOOLS`.
- GREEN: se quitó el filtro; intersección pura. Follow-on: compiler, skills, validators.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/unit/mcp/test_registry.py::test_discovered_tool_in_intersection_is_allowed -v
# RED observado: Extra items in the right set: 'crear_turno'

uv run --extra quality pytest tests/unit/mcp/test_registry.py tests/unit/skills/test_registry.py tests/unit/agent/test_context_compiler.py tests/evals/unit/test_dataset.py tests/unit/onboarding/test_validator.py -v
# GREEN: 45 passed (suite pedida; evals vive en tests/evals/unit/test_dataset.py)

uv run --extra quality pytest tests/security/test_tool_contracts.py tests/unit/mcp/test_executor.py tests/unit/workflows/appointments/test_skill.py tests/unit/skills/test_faq.py tests/unit/agent/test_harness.py -v
# 21 passed (regresión cercana; executor no modificado)

uv run --extra quality ruff check
All checks passed!

uv run --extra quality mypy src/ia_mcp/mcp/registry.py src/ia_mcp/agent_runtime/context_compiler.py src/ia_mcp/skills/registry.py src/ia_mcp/skills/appointments.py src/ia_mcp/evals/validator.py src/ia_mcp/onboarding/validator.py src/ia_mcp/configuration/models.py
Success: no issues found in 7 source files
```

## Criterios

Autorización por intersección server ∩ tenant ∩ skill, sin catálogo cerrado. Criterios AC-P09-* pendientes del brief P09-T01 si aterriza después.

## Decisiones

Ninguna ADR nueva. `KNOWN_TOOLS` permanece como set canónico para workflows/fakes. `TenantConfig.enabled_tools` (default vacío) es el allowlist de tenant que el skill appointments prefiere cuando está presente.

## Desviaciones

Ninguna funcional. El validador de evals vive en `tests/evals/unit/test_dataset.py`, no en `tests/unit/evals/test_validator.py`. No se editó `executor.py` (P09-T04) ni se añadió cliente SSE (P09-T03).

## Riesgos residuales

El executor sigue despachando solo los seis nombres canónicos; un tool descubierto autorizado por el registry aún no tiene handler (P09-T04). Sin `server_tools` inyectado, el compiler usa `tenant_tools` como catálogo de server.
