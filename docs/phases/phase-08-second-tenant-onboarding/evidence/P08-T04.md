# Evidencia P08-T04 — Segundo tenant

**Estado:** in_review  
**Baseline registrado:** `9bbb790` (`chore: accept P08-T03; unlock P08-T04` / `origin/main`)  
**Implementación T03 en main:** `bc4f3f8`  
**Entrega:** `5d04156` · **corrección conformidad:** `fix: make onboarding runbook commands executable`  
**Entorno:** local · Python 3.13.12 · PostgreSQL `ia_mcp_p02_t03`

## Resultado

Tenant B sintético bajo `tenants/fixtures/tenant-b/` (tone formal, required fields con `practitioner`, tools search/get/create, MCP `fake-appointments-b`, corpus `canary-tenant-b`). El flujo validate → provision → ingest canario → preflight → activate reutiliza T01–T03. Retrieval de B no devuelve canario A. Disable/rollback deja A intacto. `scripts/check_tenant_specific_core.py --base 9bbb790` pasa sobre este changeset (packages/tests/adapters/docs/scripts) y falla ante un branch por slug/nombre en Core.

## Archivos

- `tenants/fixtures/tenant-b/knowledge/hours-b.txt`
- `tenants/fixtures/tenant-b/knowledge/manifest.yaml`
- `tests/e2e/test_second_tenant.py`
- `tests/unit/scripts/test_check_tenant_specific_core.py`
- `scripts/check_tenant_specific_core.py`
- `src/ia_mcp/onboarding/cli.py`
- `docs/phases/phase-08-second-tenant-onboarding/runbook-onboarding.md`
- `docs/phases/phase-08-second-tenant-onboarding/evidence/P08-T04.md`

## TDD

- RED: `ModuleNotFoundError: No module named 'scripts.check_tenant_specific_core'`; `hours-b.txt` ausente (`AssertionError: assert False` sobre `CANARY_B_FILE.is_file()`).
- GREEN: corpus canario B + checker + E2E A/B; suite requerida en verde.
- RED (conformidad): `python -m ia_mcp.onboarding provision|preflight|activate|disable` → `onboarding service is not configured` / exit 2 (`test_runbook_cli_commands_do_not_exit_unconfigured`).
- GREEN (conformidad): `default_onboarding_service()` lee `DATABASE_URL` (misma URL que el E2E) y construye `TenantOnboardingService`; los comandos del runbook ya no salen por 2.
- RED (re-review AC-P08-010): `src/ia_mcp/skills/cli.py` con `if tenant.tenant_slug == "tenant-b"` → 0 hallazgos porque el checker exceptuaba todo `**/cli.py`.
- GREEN (re-review): la excepción es solo `src/ia_mcp/onboarding/cli.py`; `test_core_cli_outside_onboarding_is_still_scanned` falla cerrado.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/e2e/test_second_tenant.py tests/security/test_onboarding.py tests/unit/scripts/test_check_tenant_specific_core.py tests/integration/onboarding/test_provision.py::test_cli_provision_and_disable -v
20 passed in 2.88s

uv run python scripts/check_tenant_specific_core.py --base 9bbb790
exit 0

uv run --extra quality ruff check src/ia_mcp/onboarding/cli.py scripts/check_tenant_specific_core.py tests/unit/scripts/test_check_tenant_specific_core.py tests/e2e/test_second_tenant.py
All checks passed!

uv run --extra quality mypy src/ia_mcp/onboarding/cli.py scripts/check_tenant_specific_core.py
Success: no issues found in 2 source files
```

## Diff reasoning

`--base 9bbb790` cubre este changeset. Archivos: package B, tests, script, runbook, evidence y el adapter CLI (`src/ia_mcp/onboarding/cli.py`). El checker trata **solo** `src/ia_mcp/onboarding/cli.py` y `**/adapters/**` como adapters permitidos; cualquier otro `src/ia_mcp/**/cli.py` (p. ej. `skills/cli.py`) sigue siendo Core y se escanea. Rechaza `if tenant.tenant_slug == "tenant-b"` / `institution_name == "..."`. Las comparaciones de aislamiento (`row.slug != identity.tenant_slug`) no fallan el gate. No se reescribió el dominio T01–T03 ni se cableó `create_app`.

## Criterios

| Criterio | Prueba | Resultado |
|---|---|---|
| AC-P08-001 | `test_invalid_package_fails_before_persist` | pass |
| AC-P08-002 | replay en `test_second_tenant_validate_provision_preflight_activate_isolates` | pass |
| AC-P08-003 | `test_inconsistent_config_fails_validation` | pass |
| AC-P08-004 | `test_secret_reference_is_validated_without_printing_value` | pass |
| AC-P08-005 | corpus B → `canary-tenant-b`, nunca `canary-tenant-a` | pass |
| AC-P08-006 | MCP/tools B = `fake-appointments-b` + search/get/create vía config/integration | pass |
| AC-P08-007 | `test_stale_preflight_hash_cannot_activate` | pass |
| AC-P08-008 | activar B no muta run/job/config de A | pass |
| AC-P08-009 | disable B bloquea put; claim despacha A; A active | pass |
| AC-P08-010 | checker falla slug branch sintético, escanea Core `**/cli.py` que no sea onboarding, y pasa `--base 9bbb790` | pass |

## Decisiones y desviaciones

Ninguna ADR nueva. `create_onboarding_router` no se cableó en `create_app`. El CLI default no inventa auth federada: sigue usando `--principal-id` / `--role`. Factory mínima: `DATABASE_URL` → `create_async_engine` → `TenantOnboardingService`. Ingest de knowledge B sólo para el canario sintético. Puertos de preflight secrets/MCP/eval/observability son genéricos en el E2E; el CLI usa los defaults fail-closed de T03.

## Riesgos residuales

- Preflight MCP/eval/secrets reales siguen siendo puertos inyectados; el default de T03 permanece fail-closed fuera de este E2E.
- G4 / integración real de un segundo tenant institucional siguen fuera de alcance (fixtures sintéticos).
