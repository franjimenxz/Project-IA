# Evidencia P08-T01 — Tenant package validator

**Estado:** in_review  
**Implementación:** `implementation/P08-T01-package-validator`  
**Rojo observado:** `ModuleNotFoundError: No module named 'ia_mcp.onboarding'`  
**Calidad:** `content_hash` incluye URIs `secret_reference`/`credentials_reference`; literales no salen en el reporte.

## Resultado

`validate_package(Path) -> ValidationReport` carga un package declarativo (`tenant.yaml`, `config.yaml`, `policies/`, `knowledge/manifest.yaml`, `integrations.yaml`, `evals.jsonl`), aplica JSON Schema/Pydantic estricto y validación cruzada, y no persiste ni resuelve secret values. El CLI `python -m ia_mcp.onboarding validate <path>` imprime el reporte redactado.

## Archivos

- `src/ia_mcp/onboarding/models.py`
- `src/ia_mcp/onboarding/loader.py`
- `src/ia_mcp/onboarding/validator.py`
- `src/ia_mcp/onboarding/cli.py`
- `src/ia_mcp/onboarding/__init__.py`
- `src/ia_mcp/onboarding/__main__.py`
- `src/ia_mcp/onboarding/schemas/tenant-package.schema.json`
- `tests/unit/onboarding/test_validator.py`
- `tests/unit/onboarding/helpers.py`
- `tenants/fixtures/tenant-b/`

## TDD

- RED: `pytest tests/unit/onboarding/test_validator.py::test_package_rejects_secret_value` y `::test_package_rejects_invalid_cross_file_config` → `ModuleNotFoundError: No module named 'ia_mcp.onboarding'`.
- GREEN: implementación mínima de models/loader/validator/CLI, fixture sintético tenant-b (sin secret values ni PDFs) y suite unitaria.
- RED (calidad): `test_content_hash_changes_when_secret_reference_uri_changes` → hashes idénticos porque el hash omitía URIs de referencia.
- GREEN (calidad): el hash incluye `sm://` identifiers; literales siguen fuera del reporte.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/unit/onboarding -v
10 passed in 0.11s

uv run python -m ia_mcp.onboarding validate tenants/fixtures/tenant-b
exit 0
{
  "valid": true,
  "schema_version": 1,
  "package_path": ".../tenants/fixtures/tenant-b",
  "content_hash": "17354bf76d460e76f21c19bf07a1c31e00cf35ccb863b5ce69a99373ec022422",
  "errors": [],
  "warnings": []
}

uv run --extra quality ruff check src/ia_mcp/onboarding tests/unit/onboarding
All checks passed!

uv run --extra quality mypy src/ia_mcp/onboarding
Success: no issues found in 6 source files
```

## Reporte inválido (redactado)

Paquete con `integrations.token = "plain-secret"`. El valor no aparece en el reporte:

```text
{
  "valid": false,
  "schema_version": 1,
  "errors": [
    {
      "path": "integrations.yaml.token",
      "code": "secret_literal",
      "message": "secret values are forbidden"
    },
    {
      "path": "integrations.yaml.token",
      "code": "extra_forbidden",
      "message": "extra fields are forbidden: token"
    }
  ]
}
```

Schema publicado: `src/ia_mcp/onboarding/schemas/tenant-package.schema.json` (`additionalProperties: false`, `schema_version` const 1, referencias URI).

## Criterios

| Criterio | Prueba | Resultado |
|---|---|---|
| AC-P08-001 | `test_package_rejects_secret_value`, `test_package_rejects_extra_fields`, `test_package_rejects_unsupported_schema_version`; `validate_package` no persiste | pass |
| AC-P08-003 | `test_package_rejects_invalid_cross_file_config`, `test_package_rejects_tool_for_disabled_skill`, `test_package_rejects_duplicate_channel_mapping`, `test_package_rejects_invalid_manifest_checksum` | pass |
| AC-P08-004 | `test_secret_reference_is_validated_without_printing_value`, `test_content_hash_changes_when_secret_reference_uri_changes`; reporte sin valor literal | pass |

## Decisiones y desviaciones

Ninguna ADR nueva. El loader acepta YAML de bloque y JSON (subconjunto YAML 1.2) sin añadir PyYAML a `pyproject.toml` (archivo fuera de scope). Las referencias se validan por forma URI; no se resuelven contra un secret store. `content_hash` incorpora esas URIs como identificadores; literales de secreto se omiten del payload hasheado y no se imprimen.

## Riesgos residuales

El schema JSON viaja junto al módulo y se lee en desarrollo vía `pythonpath`; no se declaró `package-data` en `pyproject.toml` (fuera de archivos permitidos). P08-T02 debe consumir `TenantPackage`/`ValidationReport` sin persistir packages inválidos.

## Handoff

**Próxima tarea desbloqueada:** P08-T02  
**Revisión requerida:** conformidad y calidad
