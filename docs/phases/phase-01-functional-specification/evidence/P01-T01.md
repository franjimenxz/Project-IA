# Evidencia P01-T01 — Validador documental

**Estado:** accepted  
**Implementación:** `e6b5736`, `655dae8`, `371aa73`  
**Revisión:** conformidad y calidad aprobadas después de una ronda de corrección

## Resultado

`scripts/check_docs.py` valida IDs de definición, links Markdown locales, placeholders fuera de `docs/templates`, contenido obligatorio de briefs y gates. El CLI expone `--ids`, `--links`, `--placeholders`, `--briefs`, `--gates` y `--all`, devuelve hallazgos `archivo:línea` y usa exit 1 ante errores.

## Evidencia TDD

- RED inicial: import de `scripts.check_docs` inexistente al ejecutar el primer test de IDs duplicados.
- RED de regresión: tests demostraron falsos positivos sobre pares UC registry/detail, secciones vacías, tablas `ID` no normativas y directorios `templates` no canónicos.
- GREEN final: `23 passed`.

## Verificación

```text
.venv/bin/python -m pytest tests/docs -v
23 passed

.venv/bin/ruff check scripts tests/docs
All checks passed!

.venv/bin/python scripts/check_docs.py --all docs
exit 0

git diff --check
exit 0
```

## Criterios

AC-P01-001–AC-P01-005 cubiertos por validación ejecutable y diagnósticos reproducibles.

## Revisión y riesgos

- Critical/Important abiertos: ninguno.
- Minor diferido para revisión integral: destinos Markdown entre angle brackets con espacios pueden requerir soporte adicional.
- Próxima tarea desbloqueada: P01-T02.
