# Evidencia P01-T02 — Validador de trazabilidad

**Estado:** accepted  
**Implementación:** `154d26a`  
**Revisión:** conformidad y calidad aprobadas sin hallazgos

## Resultado

`scripts/check_traceability.py` compara RF/RNF `must` del catálogo contra celdas de la matriz, expande IDs y rangos completos/abreviados, valida referencias AC contra sus definiciones reales y emite diagnósticos determinísticos `archivo:línea`.

## Evidencia TDD

- RED inicial: `scripts.check_traceability` inexistente.
- RED de comportamiento: tests demostraron ausencia de soporte para IDs individuales, rangos, tablas estrictas y referencias AC.
- GREEN final: `32 passed` para la suite acumulada.

## Verificación

```text
.venv/bin/python -m pytest -v
32 passed

.venv/bin/ruff check scripts tests/docs
All checks passed!

.venv/bin/python scripts/check_traceability.py
exit 0

git diff --check
exit 0
```

## Criterios

P01-DOC-003 y AC-P01-002 cubiertos. Casos verificados: requisito faltante, ID individual, rangos RF/RNF completos y abreviados, prioridades no bloqueantes, narrativa fuera de tablas, rangos AC y referencias AC inexistentes.

## Revisión y riesgos

- Critical/Important/Minor abiertos: ninguno.
- El parser limita deliberadamente la entrada a tablas Markdown con separador válido.
- Próxima tarea desbloqueada: P01-T03.
