# Evidencia P01-T03 — Gate documental en CI

**Estado:** accepted  
**Implementación:** `87fc14d`  
**Revisión:** conformidad y calidad aprobadas sin hallazgos

## Resultado

El workflow de pull request usa Python 3.13, instala el extra bloqueado `quality` y ejecuta los validadores documental y de trazabilidad, la suite Pytest y Ruff. `pyproject.toml` fija `pytest==9.1.1` y `ruff==0.16.5`.

## Evidencia TDD

- RED de instalabilidad: `.[quality]` no era instalable porque faltaba `pyproject.toml`.
- RED del comando CI: el ejecutable `pytest` no resolvía `scripts` desde la raíz.
- GREEN final: metadata instalable, recolección equivalente a CI y `34 passed`.

## Verificación

```text
python --version
Python 3.13.12

python -m pip install --dry-run ".[quality]"
Would install ia-mcp-0.0.0

python scripts/check_docs.py --all docs
exit 0

python scripts/check_traceability.py
exit 0

pytest tests/docs -v
34 passed

ruff check scripts tests/docs
All checks passed!

git diff --check
exit 0
```

## Criterios

P01-DOC-001, P01-DOC-002, P01-DOC-003, P01-DOC-005 y AC-P01-002 se ejecutan en cada pull request.

## Revisión y riesgos

- Critical/Important/Minor abiertos: ninguno.
- La ejecución remota del workflow se observará al abrir el primer pull request; trigger y comandos fueron verificados en diff y su equivalente local terminó verde.
- Próxima tarea desbloqueada: P02-T01.
