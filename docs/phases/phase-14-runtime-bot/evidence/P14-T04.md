# Evidencia P14-T04 — Wiring del runtime

**Tarea:** P14-T04 — Wiring del runtime  
**Estado:** accepted  
**Implementación:** `1473e5f`  
**Rama:** `cursor/p14-t04-runtime-wiring-995f`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/97  
**Revisión:** two-stage PASS at `1473e5f`

## Resultado

`build_runtime` instancia `GeminiLLM(transport=UrllibGeminiTransport(), api_key=...)` si `environment_variable_for("sm://platform/llm/gemini")` tiene valor. Si no, `FakeLLM`. Knowledge: `LabKnowledgeSearch` si hay packages dir, si no `EmptyKnowledgeSearch`. Compiler: `server_tools=frozenset({"appointments.search", "appointments.get"})`. Sigue síncrono. No llama `SecretResolver.resolve`. Cero ramas por slug.

## Revisión 1 — Conformidad PASS

Solo `composition.py` y `test_composition.py`. AC-P14-008, AC-P14-009, AC-P14-010. Sin tablero, sin reimplementar Gemini/knowledge, sin clave real.

## Revisión 2 — Calidad PASS

Fail-closed si la clave o el dir faltan o están en blanco. Tests con `test-not-a-secret`. Anotaciones `LLMPort` / `KnowledgeSearch` para mypy. Suite local 27 passed. CI verde.

## Verificación

```text
PYTHONPATH=src pytest tests/unit/api/test_composition.py -v
# 27 passed
ruff check src/ia_mcp/api/composition.py tests/unit/api/test_composition.py
```

## Riesgos residuales

Sin la variable de entorno el proceso sigue en `FakeLLM`. El JSON de decisión de Gemini depende de `OUTPUT_CONTRACT` (T01). `functionDeclarations` solo llevan `name`.

## Próximo paso

Fase 14 T01–T04 accepted. El operador exporta `IA_MCP_SECRET_PLATFORM_LLM_GEMINI` para la demo.
