# Evidencia P14-T03 — FAQ anuncia herramientas de lectura

**Tarea:** P14-T03 — FAQ anuncia herramientas de lectura  
**Estado:** accepted  
**Implementación:** `47081d9`  
**Rama:** `cursor/p14-t03-faq-read-tools-70ff`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/92  
**Revisión:** two-stage PASS at `47081d9`

## Resultado

`FAQSkill.allowed_tools` intersecta `config.enabled_tools` con `{appointments.search, appointments.get}`. `ContextCompiler` usa `config.enabled_tools` como término tenant de `tool_registry.available`. `server_tools` acepta `Mapping[UUID, frozenset[str]] | frozenset[str] | None` (un `frozenset` es catálogo de proceso). `SkillRegistry` registra `FAQSkill` en lugar del stub `_FaqSkill`. Isolation A/B: enabled_tools de un tenant no aparecen en el compile del otro.

## Revisión 1 — Conformidad PASS

AC-P14-006, AC-P14-007, AC-P14-010. `test_faq_exposes_no_tools` sigue vacío con config sin tools. Search+create anuncia solo search. Sin `composition.py`, sin mutaciones, sin tablero.

Desviación: el brief no listaba `src/ia_mcp/skills/registry.py`. Justificada: `compile` y `TenantToolExecutors` resuelven FAQ por el registry; `_FaqSkill.allowed_tools` devolvía siempre `frozenset()` y AC-P14-007 no podía pasar. `file-map` asigna FAQ read tools en `skills/` a P14-T03.

## Revisión 2 — Calidad PASS

Sin ramas por slug. `TenantContext` en el lookup de config. El constructor sigue aceptando `tenant_tools` (callers existentes). CI de #92 verde. Suite local 49 passed (FAQ, compiler, isolation, registry, harness, loop).

## Verificación

```text
PYTHONPATH=src pytest tests/unit/skills/test_faq.py tests/unit/agent/test_context_compiler.py tests/security/test_faq_tools_tenant_isolation.py tests/unit/skills/test_registry.py tests/unit/agent/test_harness.py tests/unit/agent/test_harness_loop.py -q
# 49 passed
ruff check src/ia_mcp/skills/faq.py src/ia_mcp/skills/registry.py src/ia_mcp/agent_runtime/context_compiler.py tests/unit/skills/test_faq.py tests/unit/agent/test_context_compiler.py tests/security/test_faq_tools_tenant_isolation.py
# All checks passed
mypy src/ia_mcp/skills/faq.py src/ia_mcp/skills/registry.py src/ia_mcp/agent_runtime/context_compiler.py
# Success: no issues found in 3 source files
```

## Riesgos residuales

`self._tenant_tools` queda asignado y no se lee (el brief pide conservar el parámetro). `build_runtime` sigue con `server_tools=None` hasta T04. El anuncio real en proceso espera el wiring.

## Próximo paso

P14-T01 sigue `in_progress`. P14-T04 sigue `blocked` hasta T01 accepted.
