# Evidencia P14-T02 — Knowledge de laboratorio

**Tarea:** P14-T02 — Knowledge de laboratorio  
**Estado:** accepted  
**Implementación:** `83a3f22`  
**Rama:** `cursor/p14-t02-lab-knowledge-c111`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/89  
**Revisión:** two-stage PASS at `83a3f22`

## Resultado

`LabKnowledgeSearch` lee `{packages_dir}/{tenant.tenant_slug}/knowledge/*.txt`. `search` es async y recibe `KnowledgeQuery`. Hits con `source_id` de archivo, `document_id` `uuid5` estable, `document_version=1`, `page=1`. Ranking por overlap de tokens y `query.limit`. Directorio ausente o query sin overlap → `()`. No lee el directorio UUID. `EmptyKnowledgeSearch` y `composition.py` intactos.

## Revisión 1 — Conformidad PASS

Solo `lab_search.py` y los dos tests del brief. AC-P14-005 y AC-P14-010. Path por slug. `TenantContext` en el boundary. Sin embeddings, PDF, secretos ni tablero.

## Revisión 2 — Calidad PASS

Isolation A/B con canarios. Fail-closed si no hay dir. Sin ramas por institución en Core. Tests: rojo `ModuleNotFoundError`, luego 6 passed. Residual: T04 todavía debe instanciar esta clase en `build_runtime`.

## Verificación

```text
uv run pytest tests/unit/knowledge/test_lab_search.py tests/security/test_lab_knowledge_isolation.py -v
# 6 passed
uv run ruff check src/ia_mcp/knowledge/lab_search.py tests/unit/knowledge/test_lab_search.py tests/security/test_lab_knowledge_isolation.py
```

## Próximo paso

P14-T01 y P14-T03 siguen `in_progress`. P14-T04 sigue `blocked`.
