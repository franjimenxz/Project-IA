# Evidencia P14-T01 — Adaptador Gemini

**Tarea:** P14-T01 — Adaptador Gemini  
**Estado:** accepted  
**Implementación:** `c9c23b5`  
**Rama:** `cursor/p14-t01-gemini-adapter-245d`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/93  
**Revisión:** two-stage PASS at `c9c23b5`

## Resultado

`GeminiLLM` implementa `LLMPort`. `generate` es async y lanza `LLMError(code="provider_unavailable")`. Transport inyectable; `UrllibGeminiTransport` usa `x-goog-api-key` y timeout 10s. Modelo por defecto `gemini-3.5-flash`. `systemInstruction` es solo Core. `tone` y `tenant_instructions` van en partes de usuario distintas. Una parte `OUTPUT_CONTRACT` (aparte del Core) pide JSON `{kind, text, source_ids}` o un `functionCall`. `ToolCallProposal.name`. Sin `composition.py` ni `FakeLLM`. Tests con `api_key="test-not-a-secret"`. Sin red real.

## Revisión 1 — Conformidad PASS

AC-P14-001 a AC-P14-004 y AC-P14-010. Archivos del brief. Cero ramas por slug. `tenant_id` y slug no van en el body.

## Revisión 2 — Calidad PASS

El parser de texto exigía JSON y el body no lo pedía. Corregido en el mismo PR (`fea3cf2`): parte `OUTPUT_CONTRACT` sin concatenar al Core y sin `responseMimeType` (para no romper `functionCall`). Suite local 17 passed. CI verde.

## Verificación

```text
PYTHONPATH=src pytest tests/unit/llm/test_gemini.py tests/security/test_gemini_tenant_isolation.py -v
# 17 passed
ruff check src/ia_mcp/llm tests/unit/llm tests/security/test_gemini_tenant_isolation.py
```

## Riesgos residuales

`functionDeclarations` solo llevan `name`. `functionResponse` se adjunta al mismo turno de usuario que el query. El JSON de decisión depende de que el modelo siga `OUTPUT_CONTRACT`. T04 no debe reimplementar el adaptador.

## Próximo paso

P14-T04 `ready`.
