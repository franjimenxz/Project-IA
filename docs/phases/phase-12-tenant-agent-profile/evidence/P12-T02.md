# Evidencia P12-T02 — Contrato y cableado del perfil

**Tarea:** P12-T02 — Contrato y cableado del perfil  
**Estado:** accepted  
**Implementación:** `632d02b`  
**Rama:** `cursor/p12-t02-profile-contract-da94`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/81  
**Revisión:** two-stage PASS at `632d02b`  
**Agente:** `bc-7c0c3b6f-30a9-50d7-b095-9d4e35f2da94`

## Resultado

`AgentConfig.instructions` opcional (máx. 2000). `LLMRequest.tone` / `tenant_instructions` aditivos. `_policies` proyecta `tone` siempre e `instructions` sólo si hay texto. El `generate` del harness copia el `AgentConfig` capturado en `_get_config`. Core viaja solo en `LLMRequest.instructions`. Schema y fixture de tenant B sin secretos. `FakeLLM` de producción no se tocó.

## Revisión 1 — Conformidad PASS

Solo los archivos del brief. `ports.py`, `onboarding/models.py`, `service.py`, tablero y ADRs intactos. Test-first: `cef136f` (tests) antes de `632d02b` (feat). AC-P12-002–008. CI `platform-quality` y `documentation-quality` verdes en PR #81.

## Revisión 2 — Calidad PASS

Extensión aditiva. Isolation A/B con canarios. Knowledge no pisa el perfil. Cero ramas por slug (`find_slug_branches`). Fixture sin secretos. Residual: un solo `generate` en `main`; si aterriza el loop de Fase 11, cada uno debe copiar el mismo perfil. `FakeLLM` sigue ignorando el request. El harness captura config al inicio; `compile` puede reléer (patrón previo).

## Verificación

```text
# PR #81 platform-quality success; suites T02 61 passed (evidencia implementador)
python3 scripts/check_docs.py --all docs
python3 scripts/check_traceability.py
```

## Próximo paso

Fase 12 T01–T02 accepted. P05 sigue `blocked` por EXT.
