# Evidencia P12-T02 — Contrato y cableado del perfil

**Tarea:** P12-T02 — Contrato y cableado del perfil  
**Estado:** in_review  
**Implementación:** `feat: copy tenant agent profile to every LLM request`  
**Rama:** `cursor/p12-t02-profile-contract-da94`  
**Commit feat:** `632d02b`

## Resultado

`AgentConfig` declara `instructions` opcional (máximo 2000). `LLMRequest` recibe `tone` y `tenant_instructions` con default. `_policies` proyecta `tone` siempre e `instructions` sólo si hay texto. El único `generate` del harness copia el `AgentConfig` capturado para el `TenantContext` del turno. `CORE_INSTRUCTIONS` viaja solo en `LLMRequest.instructions`. El schema admite `agent.instructions` opcional. La fixture de tenant B declara instrucciones de política sin secretos. `FakeLLM` de producción no se tocó.

## Criterios

AC-P12-002, AC-P12-003, AC-P12-004, AC-P12-005, AC-P12-006, AC-P12-007, AC-P12-008.

## Verificación

Python 3.13.15, extra `quality` (`pytest==9.1.1`, `ruff==0.16.5`, `mypy==2.3.1`).

Rojo previo (contrato ausente, no ImportError): 24 failed / 37 passed. Causas: `instructions` extra_forbidden en `AgentConfig`; `LLMRequest` sin `tone` / `tenant_instructions`; schema y fixture de tenant B sin la clave.

```text
$ pytest tests/unit/configuration/test_agent_config.py tests/unit/agent/test_models.py tests/unit/agent/test_context_compiler.py tests/unit/agent/test_harness.py tests/unit/onboarding/test_validator.py tests/security/test_agent_profile_isolation.py -v
61 passed in 0.32s
exit: 0

$ pytest tests/unit/agent/test_harness.py tests/integration/mvp/test_faq_turn.py -v
ERROR: file or directory not found: tests/integration/mvp/test_faq_turn.py
exit: 4

$ pytest tests/unit/agent/test_harness.py -v
13 passed in 0.22s
exit: 0

$ ruff check src tests
All checks passed!
exit: 0

$ mypy src/ia_mcp/configuration src/ia_mcp/agent_runtime src/ia_mcp/onboarding
Success: no issues found in 24 source files
exit: 0
```

`tests/integration/mvp/test_faq_turn.py` no existe en este árbol. La regresión FAQ se cubre con `tests/unit/agent/test_harness.py` (expectativas existentes sin cambio) y no se inventó el archivo de integración.

## Residuales

- El harness de esta rama tiene un solo `generate`. Si el loop de Fase 11 aterriza, cada `generate` del turno debe copiar el mismo perfil.
- `FakeLLM` de producción sigue ignorando el request: el perfil es observable en el contrato, no en la prosa del fake.
- Adapter LLM de producción y retrieval real siguen abiertos.
- El tablero, el índice de ADRs y el roadmap no se editaron.

## Próximo paso

Revisión de dos etapas del coordinador. No hay otra tarea de implementación en Fase 12.
