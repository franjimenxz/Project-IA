# Evidencia P12-T01 — ADR-008 y documentación de fase

**Tarea:** P12-T01 — ADR-008 y documentación de fase  
**Estado:** in_review  
**Implementación:** `docs: define tenant agent profile and its path to the model`  
**Rama:** `cursor/p12-t01-architecture-docs-7207`

## Resultado

ADR-008 (estado `proposed`) y la Fase 12 documentan el hueco entre `AgentConfig.tone` y el `LLMRequest`, y deciden el cierre: `instructions` opcional (máximo 2000 caracteres) en `AgentConfig`, campos aditivos `tone` / `tenant_instructions` en `LLMRequest`, compiler que proyecta el perfil en `policies["agent"]` y harness que lo copia en cada `generate` sin concatenarlo con `CORE_INSTRUCTIONS`. Cada hecho de estado actual del ADR cita archivo y línea del commit base `209d850`. Sin código de producción. El brief de P12-T02 queda listo para un solo implementador.

## Criterios

AC-P12-001 satisfecho. T01 define AC-P12-001–AC-P12-008. T02 cubre AC-P12-002–AC-P12-008.

## Verificación

Python 3.13.15, extra `quality` (`pytest==9.1.1`, `ruff==0.16.5`).

```text
$ python scripts/check_docs.py --all docs
exit: 0

$ python scripts/check_traceability.py
exit: 0

$ pytest tests/docs -q
............................................                             [100%]
44 passed in 1.53s
exit: 0

$ ruff check scripts tests/docs
All checks passed!
exit: 0
```

## Residuales

- ADR-008 queda `proposed` hasta las dos revisiones del coordinador.
- El índice de ADRs, el roadmap y la matriz de trazabilidad no se editaron; los actualiza el coordinador al aceptar T01.
- El harness de esta rama tiene un solo `generate` (`src/ia_mcp/agent_runtime/harness.py:139`). T02 cablea ese constructor. Si el loop de Fase 11 aterriza, el ADR exige el mismo perfil en cada `generate`.
- Adapter LLM de producción y retrieval real siguen abiertos.

## Próximo paso

P12-T02 `ready` en brief; la ejecución la desbloquea el coordinador tras aceptar T01. P05-T01–T04 siguen bloqueadas por EXT.
