# Evidencia P13-T01 — Páginas HTML de instituciones y chat de prueba

**Tarea:** P13-T01 — Páginas HTML de instituciones y chat de prueba  
**Estado:** accepted  
**Implementación:** `a92d9af`  
**Rama:** `cursor/p13-t01-instituciones-html-ef5c`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/91  
**Revisión:** two-stage PASS at `a92d9af`

## Resultado

`InstitucionForm` / `write_lab_package` escriben un package que `validate_package` acepta (canal `simulated`, namespace = slug, policies por skill, `evals.jsonl` vacío, `instructions=""` omitido). `lab_enable` es idempotente, audit `lab_enable`, solo development/test; deja `capture()` usable sin ser el activate de preflight. HTML simple en `/admin/instituciones` y `/admin/instituciones/{slug}/chat`. POST de chat llama `AgentHarness.handle_message` con el `TenantContext` de ese slug; `channel_integration_id` se lee en el request desde SQL. Auth ADR-007. Alta/lista/`lab_enable` exigen `platform_admin`. Rutas ausentes en production. Cero ramas por slug en Core. Cero secretos en HTML/fixtures.

## Revisión 1 — Conformidad PASS

Archivos del brief: package writer, `lab_enable`, router HTML, templates, `GET /v1/admin/tenants`, suites unitarias y de aislamiento. AC-P13-002 a AC-P13-008. `TenantContext` en el boundary del chat. `composition.py` solo recibe `packages_dir` (compatible con T04). Sin campos institucionales nuevos.

## Revisión 2 — Calidad PASS

El primer HEAD enviaba `FormData` (multipart) mientras el servidor parsea `application/x-www-form-urlencoded` con `parse_qs`. Corregido en el mismo PR (`URLSearchParams` + `Content-Type: application/x-www-form-urlencoded`). Suite local 27 passed. CI `platform-quality`, `database-quality`, `documentation-quality`, `quality-evidence` y GitGuardian success.

## Verificación

```text
pytest tests/unit/onboarding/test_lab_package.py tests/unit/onboarding/test_lab_enable.py tests/unit/api/test_instituciones_html.py tests/security/test_instituciones_isolation.py -v
# 27 passed
ruff check src/ia_mcp/api src/ia_mcp/onboarding tests/unit/api/test_instituciones_html.py tests/unit/onboarding/test_lab_package.py tests/unit/onboarding/test_lab_enable.py tests/security/test_instituciones_isolation.py
```

## Riesgos residuales

GET HTML sigue exigiendo `Authorization` (igual que `/admin/runs/{run_id}`). La caja de token en la página solo sirve si la carga ya llevó header. Tests de idempotencia/`capture()` de `lab_enable` usan store en memoria, no el UPDATE SQL vivo.

## Próximo paso

Fase 13 T01 accepted. El bot de demo sigue en P14-T01 y P14-T04.
