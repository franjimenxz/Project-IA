# Evidencia P13-T01 — Páginas HTML de instituciones y chat de prueba

**Tarea:** P13-T01 — Páginas HTML de instituciones y chat de prueba  
**Estado:** in_review  
**Rama:** `cursor/p13-t01-instituciones-html-ef5c`  
**Agente:** `bc-45c38cc8-708f-542e-94bb-3f7d1821ef5c`

## Resultado

`InstitucionForm` / `write_lab_package` escriben un package que `validate_package` acepta (canal `simulated`, namespace = slug, policies por skill, `evals.jsonl` vacío, `instructions=""` omitido). `lab_enable` es idempotente, audit `lab_enable`, solo development/test; deja `capture()` usable sin ser el activate de preflight. HTML simple en `/admin/instituciones` y `/admin/instituciones/{slug}/chat` (aspecto WhatsApp). POST de chat llama `AgentHarness.handle_message` con el `TenantContext` de ese slug; `channel_integration_id` se lee en el request desde SQL, no del mapa de startup. Auth ADR-007 (`Authorization`). Alta/lista/`lab_enable` exigen `platform_admin`. Rutas ausentes en production. Cero `if tenant.slug` en Core. Cero secretos en HTML/fixtures.

## Verificación

```text
pytest tests/unit/onboarding/test_lab_package.py tests/unit/onboarding/test_lab_enable.py tests/unit/api/test_instituciones_html.py tests/security/test_instituciones_isolation.py -v
# 27 passed

ruff check src/ia_mcp/api src/ia_mcp/onboarding tests/unit/api/test_instituciones_html.py tests/unit/onboarding/test_lab_package.py tests/unit/onboarding/test_lab_enable.py tests/security/test_instituciones_isolation.py
# All checks passed

mypy src/ia_mcp/api src/ia_mcp/onboarding
# Success: no issues found in 21 source files
```

## Criterios

AC-P13-002, AC-P13-003, AC-P13-004, AC-P13-005, AC-P13-006, AC-P13-007, AC-P13-008.

## Decisiones

ADR-002, ADR-007, ADR-009. Sin API ni auth inventadas.

## Desviaciones

Ninguna de contrato. El parseo HTML de formularios usa `application/x-www-form-urlencoded` sin añadir `python-multipart` (fuera de archivos permitidos / dependencias).

## Riesgos residuales

`lab_enable` SQL (UPDATE de canal `simulated` y bindings MCP) se cubre por el store real; la suite unitaria de idempotencia/`capture()` usa un doble en memoria para no exigir PostgreSQL en `tests/unit`. El bot sigue débil (`FakeLLM` intacto).

## Próximo paso

Dos revisiones del protocolo. El coordinador actualiza el tablero. El bot débil se mejora después.
