# Evidencia — Fase 13

Guardar:

- package escrito por el form con `validate_package` verde;
- `lab_enable` idempotente y `capture()` posterior;
- HTML de lista y chat;
- aislamiento A/B y 401;
- router ausente en production.

No commitear credenciales, tokens ni payloads con PII.
