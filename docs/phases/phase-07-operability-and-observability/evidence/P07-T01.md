# Evidencia P07-T01 — Telemetría correlacionada

**Estado:** in_review  
**Implementación:** `implementation/P07-T01-telemetry`  
**Rojo observado:** `ModuleNotFoundError: No module named 'ia_mcp.observability.propagation'` al recolectar las tres suites mandatorias.

## Resultado

Context inject/extract versionado (`semconv` 1.0.0, W3C `traceparent` + `X-Correlation-ID`) atraviesa HTTP, channel outbox, worker y MCP. El exporter acotado no cambia resultados de negocio. Labels de métricas droppean IDs de alta cardinalidad. Spans no incluyen content/payload/secrets.

## Evidencia TDD

- RED: `uv run --extra quality pytest tests/integration/observability/test_propagation.py tests/resilience/test_telemetry.py tests/security/test_observability.py -v` → collection error, módulo ausente.
- GREEN: 5 passed (mismo comando).

## Span tree sanitizado (HTTP inbound)

```json
[
  {
    "name": "channel.receive",
    "trace_id": "fcbbc54945324bf694e3c0005c09677d",
    "span_id": "0b7f4b928905438b",
    "parent_span_id": "d071a64012a94ac6",
    "correlation_id": "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa",
    "attributes": {},
    "links": []
  }
]
```

El test de propagación afirma un único `trace_id`/`correlation_id` para `channel.receive`, `channel.send`, `scheduler.dispatch`, `mcp.resolve` y `tool.execute`. Retry enlaza el span previo (`links`). Sin prompts, payloads ni secretos en el dump.

## Cardinality

```python
metric_labels(
    conversation_id=...,
    run_id=...,
    patient_id="pat-1",
    document_id="doc-1",
    skill="faq",
    status="ok",
    tenant_id="tenant-a",
)
# -> {'skill': 'faq', 'status': 'ok', 'tenant_id': 'tenant-a'}
```

`conversation_id`, `run_id`, `patient_id`, `document_id` y `message_id` no son labels.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/integration/observability/test_propagation.py tests/resilience/test_telemetry.py tests/security/test_observability.py -v
5 passed, 1 warning in 7.96s

uv run --extra quality ruff check src/ia_mcp/observability src/ia_mcp/api/app.py src/ia_mcp/channels/outbox.py src/ia_mcp/scheduling/worker.py src/ia_mcp/mcp/executor.py tests/integration/observability/test_propagation.py tests/resilience/test_telemetry.py tests/security/test_observability.py tests/unit/api/observability_app.py
All checks passed!

uv run --extra quality mypy src/ia_mcp/observability src/ia_mcp/api/app.py src/ia_mcp/channels/outbox.py src/ia_mcp/scheduling/worker.py src/ia_mcp/mcp/executor.py
Success: no issues found in 8 source files
```

Warning residual: `StarletteDeprecationWarning` httpx/httpx2 (ajeno).

## Criterios

| ID | Estado |
|---|---|
| AC-P07-006 | satisfied — un trace/correlation HTTP→outbox→worker→MCP |
| AC-P07-007 | satisfied — exporter caído no cambia dispatch/tool; métrica local `telemetry_exporter_failure` |
| AC-P07-008 | satisfied — métricas sin IDs de alta cardinalidad |

## Revisión y riesgos

- Critical/Important abiertos: ninguno.
- Residual: `ReminderScheduler.upsert` no inyecta carrier (fuera de hooks permitidos); el worker extrae de `payload.telemetry` o cae a `correlation_id`. P07-T02 no desbloqueado por este entregable hasta review.
- Próxima tarea desbloqueada: P07-T02 (tras aceptación; queued hasta terminar T01).
