# Evidencia P07-T01 — Telemetría correlacionada

**Estado:** in_review  
**Implementación:** `implementation/P07-T01-telemetry`  
**Rojo observado (original):** `ModuleNotFoundError: No module named 'ia_mcp.observability.propagation'`  
**Rojo review:** tests exigían `flush_telemetry`, inject en `ReminderScheduler.upsert` sin paste manual, exporter async, `set_attribute`, inbound headers ignorados.  
**Rojo re-review I1/I2:** `POST /v1/simulated/messages` con `X-Correlation-ID: bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb` devolvía `body.correlation_id` igual al header forjado y `X-Correlation-ID` de respuesta distinto (UUID de servidor). HMAC firma `account.timestamp.body` y no cubre ese header.

## Resultado

- Semantic conventions 1.0.0; inject/extract W3C `traceparent` + `X-Correlation-ID`.
- Una sola fuente de correlation por request HTTP: `TelemetryContext` / `request.state.correlation_id` del middleware. Inbound `traceparent` y `X-Correlation-ID` se ignoran también en el canal simulado firmado. Body, outbox y header de respuesta usan el mismo UUID.
- `ReminderScheduler.upsert` inyecta `payload.telemetry` (y alinea `correlation_id`) desde el contexto actual. Worker y audit usan `extract_payload`.
- Exporter async: `emit` solo encola; overflow incrementa `telemetry_exporter_dropped`; fallo incrementa `telemetry_exporter_failure` sin cambiar negocio.
- `SpanRecord.set_attribute` pasa por `span_attributes`/redact. Redactor cubre Bearer, Basic, api_key, DNI y teléfono.

## Span tree sanitizado (HTTP inbound, server-minted)

Un `GET /health/live` produce `channel.receive` con correlation/trace generados en servidor. El test de propagación afirma un único `trace_id`/`correlation_id` para `channel.receive`, `channel.send`, `scheduler.dispatch`, `mcp.resolve` y `tool.execute` **sin** reinyectar el carrier a mano después de `upsert`. Retry enlaza el span previo.

## Cardinality

```text
metric_labels(..., conversation_id, run_id, patient_id, document_id, skill=faq, status=ok, tenant_id=tenant-a)
→ {'skill': 'faq', 'status': 'ok', 'tenant_id': 'tenant-a'}
```

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/security/test_observability.py::test_signed_simulated_message_ignores_forged_correlation_header -v
FAILED — AssertionError: body.correlation_id == bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb; response X-Correlation-ID == e6c1cf49-707c-486e-bdf8-27dc44aac360

uv run --extra quality pytest tests/integration/observability/test_propagation.py tests/resilience/test_telemetry.py tests/security/test_observability.py -v
9 passed, 1 warning in 0.74s

uv run --extra quality ruff check src/ia_mcp/observability src/ia_mcp/api/app.py src/ia_mcp/api/routes/simulated.py src/ia_mcp/channels/outbox.py src/ia_mcp/scheduling/worker.py src/ia_mcp/scheduling/service.py src/ia_mcp/mcp/executor.py tests/integration/observability/test_propagation.py tests/resilience/test_telemetry.py tests/security/test_observability.py tests/unit/api/observability_app.py tests/unit/api/test_correlation.py tests/unit/observability/test_redaction.py tests/e2e/test_faq.py
All checks passed!

uv run --extra quality mypy src/ia_mcp/observability src/ia_mcp/api/app.py src/ia_mcp/api/routes/simulated.py src/ia_mcp/channels/outbox.py src/ia_mcp/scheduling/worker.py src/ia_mcp/scheduling/service.py src/ia_mcp/mcp/executor.py
Success: no issues found in 10 source files
```

Warning residual: `StarletteDeprecationWarning` httpx/httpx2 (ajeno).

E2E `tests/e2e/test_faq.py` ahora lee correlation del response header (no de `CORR_SHARED` del request). No se reejecutó el stack e2e en este turno (requiere Postgres); el caso forjado del mandato corre en `tests/security/test_observability.py`.

## Criterios

| ID | Estado |
|---|---|
| AC-P07-006 | satisfied — HTTP→outbox→upsert→worker→MCP, un trace; audit usa `extract_payload` |
| AC-P07-007 | satisfied — exporter async, overflow drop, fallo no cambia dispatch/tool |
| AC-P07-008 | satisfied — labels acotados; spans redactados; header forjado ignorado en `/v1/simulated/messages` |

## Decisiones

- Trust boundary HTTP: una correlation por request = mint del middleware. El canal simulado no adopta `X-Correlation-ID` del cliente.
- Job correlation source: `payload.telemetry` via `extract_payload` (fallback `payload.correlation_id`).

## Riesgos residuales

Ninguno abierto de review I1/I2. P07-T02 sigue queued hasta aceptación.
