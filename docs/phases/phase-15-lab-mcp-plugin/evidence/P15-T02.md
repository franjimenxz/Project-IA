# Evidencia P15-T02 — Form de laboratorio y endpoint MCP

**Tarea:** P15-T02 — Form, mapa de endpoints y chat simulado  
**Estado:** accepted  
**Implementación:** `87a21b9` (`7beb038` squash)  
**Rama:** `cursor/p15-t02-lab-form-endpoint-ea35`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/102  
**Revisión:** two-stage PASS at `7beb038`

## Resultado

`InstitucionForm.mcp_endpoint` opcional; URL `http`/`https` sin userinfo. El valor no entra al package (`additionalProperties: false`). Se persiste en `{root}/lab_mcp_endpoints.json` keyed por `mcp_server_id`. POST HTML con `app.state.lab_mcp_discoverer` (stub inyectable; sin discoverer no hay red). Tools descubiertas se unen a `enabled_tools` y `mcp_capabilities`. `faq` siempre si hay endpoint; `appointments` si hay `appointments.*`. Alta exitosa responde `303` al chat. El chat se presenta como simulación de WhatsApp (canal `simulated`). Token admin nunca en HTML. `SseLabMcpDiscoverer.list_names` devuelve `()` hasta que T03 cablee discovery real.

## Revisión 1 — Conformidad PASS

Solo archivos del brief. Interfaces `LAB_ENDPOINTS_FILE`, `LabMcpDiscoverer`, `validate_lab_mcp_endpoint`, `allowlist_entry_for`, load/write, `SseLabMcpDiscoverer`. AC-P15-001 a AC-P15-005. Sin harness, executor, composition, tablero, auth MCP inventada ni WhatsApp Cloud.

## Revisión 2 — Calidad PASS

CI verde (documentation-quality, platform-quality, database-quality, quality-evidence, GitGuardian). Suite local 43 passed. Userinfo / `file:` / `169.254.169.254` rechazados. Fallo de discovery no filtra el body upstream. Isolation A/B y token ausente siguen verdes.

## Verificación

```text
uv run pytest tests/unit/onboarding/test_lab_mcp.py tests/unit/onboarding/test_lab_package.py tests/unit/api/test_instituciones_html.py tests/security/test_instituciones_isolation.py -v
# 43 passed
uv run ruff check src/ia_mcp/onboarding/lab_mcp.py src/ia_mcp/onboarding/lab_package.py src/ia_mcp/api/routes/instituciones.py tests/unit/onboarding/test_lab_mcp.py tests/unit/onboarding/test_lab_package.py tests/unit/api/test_instituciones_html.py
```

## Riesgos residuales

- `SseLabMcpDiscoverer` es no-op (contrato T02). El POST de lab no lista tools hasta que T03 publique un discoverer en `app.state`.
- `write_lab_mcp_endpoint` corre antes de `validate_package`; un alta inválida puede dejar una clave en el mapa.
- El JS del form (`fetch` + `document.write`) no cambia la URL del browser; el HTTP sí es 303.
- Query string en la URL no se recorta; userinfo sí se rechaza.

## Próximo paso

P15-T03 `ready`: runtime (FAQ, harness, compiler, executor SSE, client `intersect_allowed`, composition merge).
