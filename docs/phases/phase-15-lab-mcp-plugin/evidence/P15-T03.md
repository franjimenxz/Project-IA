# Evidencia P15-T03 — Runtime adaptado al MCP enchufado

**Tarea:** P15-T03 — Runtime del catálogo enchufado  
**Estado:** accepted  
**Implementación:** `58dfcb5` (`9181e25` squash)  
**Rama:** `cursor/p15-t03-runtime-adapt-43bf`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/105  
**Revisión:** two-stage PASS at `9181e25` (R2 devolvió un hallazgo; re-review PASS)

## Resultado

`FAQSkill.allowed_tools` = `config.enabled_tools` (AC-P15-006). `invocable_on_turn(..., declared_for_turn=tool_names)`: nombre anunciado es invocable; create no anunciado sigue `forbidden` (AC-P15-007). Knowledge vacío + `enabled_tools` no corta el turno. `answer` tras tool ok no exige cites. `ContextCompiler(mirror_tenant_tools=True)` en `build_runtime`. `list_tools(intersect_allowed=False)` no filtra. Executor: endpoint allowlisted → SSE para cualquier name; sin endpoint → fake; endpoint no allowlisted → `forbidden`. Composition fusiona lab JSON + env (env gana). `attach_runtime` publica `RuntimeLabMcpDiscoverer`. `LiveMcpEndpointResolver` recarga el mapa en cada `for_tenant` para el 303 del mismo proceso. Cero auth inventada. Cero ramas por slug.

## Revisión 1 — Conformidad PASS

Archivos de producción del brief. Tests extra (`test_registry.py`, isolation, prompt-injection) son aserciones forzadas por AC-006/008. Tablero, form y `lab_mcp.py` intactos.

## Revisión 2 — Calidad PASS tras corrección

Hallazgo: el mapa se congelaba al arrancar. Corregido con wrapper en `composition.py` (sin editar `sqlalchemy.py`). Test `test_runtime_reloads_lab_endpoint_written_after_startup`: write JSON después de `build_runtime` → search va al transport.

CI verde. Suite local 78 passed.

## Verificación

```text
uv run --extra quality pytest tests/unit/skills/test_faq.py tests/unit/agent/test_turn_tool_surface.py tests/unit/mcp/test_executor.py tests/unit/api/test_composition.py tests/security/test_faq_tools_tenant_isolation.py tests/security/test_tool_loop_isolation.py -v
# 78 passed
uv run ruff check src/ia_mcp/skills/faq.py src/ia_mcp/agent_runtime/harness.py src/ia_mcp/agent_runtime/context_compiler.py src/ia_mcp/mcp/executor.py src/ia_mcp/mcp/client.py src/ia_mcp/api/composition.py
```

## Riesgos residuales

- Un MCP que exige auth no inventada no responde hasta P05 / EXT-003.
- Mutaciones en el chat de lab no pasan por workflow (ADR-011, no-producción).
- El snapshot `_endpoints` de SQLAlchemy sigue; el overlay en `resolve` lo cubre.

## Próximo paso

Fase 15 T01–T03 accepted. El operador hace `git pull` y reinicia uvicorn para probar el enchufe.
