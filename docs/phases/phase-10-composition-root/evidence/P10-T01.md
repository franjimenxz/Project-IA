# Evidencia P10-T01 — Runtime composition root

**Tarea:** P10-T01 — Runtime composition root  
**Estado:** accepted  
**Implementación:** `fd6d0da` merged `9380843`  
**Rama:** `implementation/P10-T01-composition`  
**Revisión:** two-stage PASS at `fd6d0da` (PR #68)

## Resultado

`create_app` adjunta el grafo de runtime solo en `development` con `DATABASE_URL`: `tenant_service`, `config_service`, `agent_harness`, `channel_integration_ids` y `tool_executor` (`TenantToolExecutors`). `test` no auto-wirea (ACK intacto). `production` no monta simulated ni fakes.

## Revisión 1 — Conformidad PASS

Alcance respetado (5 archivos de implementación). Contratos Pydantic de config intactos. Sin WhatsApp, sin REST médico, sin ramas por slug. Criterios AC-P10-001, 002, 004, 005, 006 cubiertos por tests unitarios.

AC-P10-003 no tiene test automatizado durable en el repo; el implementador lo verificó contra Postgres descartable. El coordinador no reprodujo esa corrida. Residual.

## Revisión 2 — Calidad PASS

Factory por tenant para el executor: las allowlists son datos del tenant. Resolver SQL chequea `tenant_id` + slug. Knowledge vacío fail-closed. `FakeLLM` insufficient sin secretos. Parseo de `IA_MCP_MCP_ENDPOINTS` no echoa el valor.

## Decisiones del coordinador

1. `IA_MCP_MCP_ENDPOINTS` (`server_id=endpoint`) aceptada como dato de **deployment**, indexada por el `server_id` que declara el tenant. No es `if slug ==`. Si el endpoint debe vivir en el paquete, enmienda de contrato en otra tarea.
2. `app.state.tool_executor` como factory (`TenantToolExecutors`) aceptada. Un executor compartido no puede intersecar allowlists tenant-scoped.

## Verificación del coordinador

```text
uv run --extra quality pytest tests/unit/api/test_composition.py tests/unit/api/test_app.py tests/integration/api/test_simulated_messages.py tests/unit/mcp/test_executor.py -q
# 29 passed, 1 warning in 0.49s

uv run --extra quality ruff check src/ia_mcp/api src/ia_mcp/tenancy
# All checks passed

uv run --extra quality mypy src/ia_mcp/api/composition.py src/ia_mcp/api/app.py src/ia_mcp/tenancy/adapters/sqlalchemy.py
# Success: no issues found in 3 source files
```

## Residuales

- `channel_integration_ids` es foto de startup.
- `ContextCompiler(tenant_tools={})`: schemas de tools vacíos.
- `AgentHarness` no recibe el executor; la factory no tiene consumidor HTTP.
- `create_app()` por defecto es development: un `DATABASE_URL` inválido en el shell rompe callers que no pasan `environment=`.
- `create_onboarding_router` sigue sin montar (P08-T03).
