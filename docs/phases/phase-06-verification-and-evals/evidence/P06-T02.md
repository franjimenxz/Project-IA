# Evidencia P06-T02 — Runner y scorers (review fix)

**Estado:** in_review  
**Entorno:** local · Python 3.13.12 · pytest 9.1.1 · ruff 0.16.5 · mypy 2.3.1  
**Dataset hash:** `674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b`

Los SHA de commit y del reporte smoke se registran en el tip de `implementation/P06-T02-eval-runner` (no `8180494`). Ver la sección Verificación tras el commit de fix.

## Resultado

Corrección de hallazgos de conformidad/calidad:

1. Allowlist vacía es fail-closed: tools/sources no listados fallan aunque `allowed_*` esté vacío.
2. `observe_turn` registra `result.tenant_id` (fallback al tenant pedido sólo si el result no trae id). Cross-tenant bajo A falla scoring.
3. Umbrales por categoría (fake/smoke, todos 1.0): tenant, tools, sources, policy, workflow, outcome, skill, groundedness. Un collapse a `insufficient` (outcome/groundedness 0.0) falla el gate. AC-P06-004 se mantiene: forbidden-tool crítico falla el gate con skill/groundedness = 1.0.
4. Leak tests sobre strings JSON/Markdown; valores de `config_summary` no pueden llevar `prompt`/`reasoning`/`completion`/`core_instructions`. Un no-op de `_reject_private` hace fallar esas aserciones de string.
5. Compare trata missing cases, `dataset_hash` distinto y cambio de model como regresión (exit ≠ 0).
6. `redact()` existente se aplica al JSON/Markdown; Bearer/email no aparecen en claro.

## Honestidad smoke

`--suite smoke --provider fake` **no** ejercita forbidden-tool vía Harness: `tenant_tools` está vacío, FAQ no llama appointments, y el caso `uc-08-tenant-a-tool-forbidden` pasa porque el harness no invoca la tool. La prueba de AC-P06-004 es unitaria (`test_forbidden_tool_fails_critical_case`, `test_critical_failure_fails_gate_despite_high_averages`), no el smoke.

FakeLLM: se siembra `insufficient` **sólo** cuando el caso tiene sources y espera insufficient (injection). Hits vacíos cortocircuitan en el harness. No se usa expected_outcome como script genérico para appointments/handoff.

## Umbrales

| Categoría | Umbral fake/smoke |
|---|---|
| tenant, tools, sources, policy, workflow | 1.0 |
| outcome, skill, groundedness | 1.0 (collapse no se oculta; modelo real sustituye skill/groundedness por baseline aprobado) |

## Verificación

Comandos (reproducir en el tip):

```text
uv run --extra quality pytest tests/evals/unit -v
uv run python -m ia_mcp.evals run --suite smoke --provider fake
uv run --extra quality mypy src/ia_mcp/evals tests/evals/unit
uv run --extra quality ruff check src/ia_mcp/evals tests/evals/unit
```

RED observado antes del fix: `passed is True` con `unlisted.tool` / `unlisted-source`; `observe_turn` copiaba el tenant pedido.

## Criterios

| ID | Estado |
|---|---|
| AC-P06-002 | satisfied — strings de reporte sin prompt/reasoning/completion; redact() en artefactos |
| AC-P06-003 | satisfied — exact match fail-closed, tenant observado, sin judge |
| AC-P06-004 | satisfied — unit tests; smoke no cubre forbidden-tool en harness |
| AC-P06-005 | satisfied — compare por categoría, casos faltantes y provenance |
| AC-P06-009 | satisfied — commit/dataset/model/config/environment en JSON y Markdown |

## Residuales

- Smoke FAQ-only (`AgentHarness` resuelve `faq`).
- FakeLLM: seed estrecho de insufficient con sources (injection).
- Source IDs de catálogo, no de ingestión runtime.
- Forbidden-tool en trayectoria real del harness queda fuera de smoke.

## Handoff

**Próxima tarea desbloqueada:** P06-T05 (tras T03/T04)  
**Revisión requerida:** conformidad y calidad
