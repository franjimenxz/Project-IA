# Evidencia P06-T02 — Runner y scorers

**Estado:** in_review  
**Entorno:** local · Python 3.13.12 · pytest 9.1.1 · ruff 0.16.5 · mypy 2.3.1  
**Dataset hash:** `674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b`  
**Smoke report hash:** `4a971dc57bc34450b3a75ff8a7fd5b6c13dc80a583f62737048e4c97652b48bb`

## Resultado

Runner sobre `AgentHarness` + `EvalCase` produce `ObservedTrajectory` sin prompt, completion ni reasoning privado. Scorers determinísticos (sin judge) para tenant, sources y tools; un caso crítico fallido falla el gate aunque el promedio de skill/groundedness sea alto. Reportes JSON/Markdown reproducen commit, dataset hash, model/config y environment. CLI: `run --suite smoke --provider fake` y `compare --baseline --current`.

## Evidencia TDD

- RED: `ModuleNotFoundError: No module named 'ia_mcp.evals.scorers'` y `No module named 'ia_mcp.evals.report'` al colectar `test_forbidden_tool_fails_critical_case` y `test_baseline_regression_is_reported_by_category`.
- GREEN: 32 passed en `tests/evals/unit`; smoke fake PASS, exit 0.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/evals/unit -v
32 passed
exit 0

uv run python -m ia_mcp.evals run --suite smoke --provider fake
gate: PASS (pass)
commit: 81804948bddecb9e10e786b35e4704e5e03593cc
dataset hash: 674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b
model: fake/fake-llm
config: {"config_version": 1, "suite": "smoke"}
environment: python 3.13.12 / macOS-26.5.2-arm64-arm-64bit-Mach-O
cases: 6 FAQ smoke (uc-01, uc-08 hours A/B, insufficient, injection, tool-forbidden)
exit 0

uv run --extra quality mypy src/ia_mcp/evals tests/evals/unit
Success: no issues found in 13 source files

uv run --extra quality ruff check src/ia_mcp/evals tests/evals/unit
All checks passed!
```

Compare CLI (unit): exit 1 con regresión de categoría `tools`; exit 0 si baseline y current coinciden.

## Criterios

| ID | Estado |
|---|---|
| AC-P06-002 | satisfied — trayectoria sin prompt/reasoning/completion; resumen de contexto compilado sin `core_instructions` |
| AC-P06-003 | satisfied — tenant, forbidden source y forbidden tool por exact match / set comparison, sin judge |
| AC-P06-004 | satisfied — `forbidden_tool:...` es critical failure y falla el gate con promedio skill/groundedness = 1.0 |
| AC-P06-005 | satisfied — `compare_reports` reporta regresión por categoría y CLI sale distinto de cero |
| AC-P06-009 | satisfied — JSON/Markdown incluyen commit, dataset hash, model/config y environment |

## Decisiones y desviaciones

ninguna sobre contratos T01. `ObservedTrajectory` vive en `scorers.py` para evitar ciclo runner↔scorer. Smoke usa el subconjunto FAQ del dataset porque `AgentHarness` actual resuelve skill `faq`. No se reescribieron `models.py`, `validator.py` ni `evals/datasets/mvp.jsonl`.

## Riesgos residuales

El smoke fake alinea la decisión de `FakeLLM` con `expected_outcome` para casos FAQ; no mide un modelo real. Casos appointments/handoff/scheduler quedan fuera de `--suite smoke` hasta que el harness cubra esas skills. Source IDs de eval siguen siendo fixtures de catálogo (`kb-a-hours`), no IDs de ingestión runtime.

## Handoff

**Próxima tarea desbloqueada:** P06-T05 (tras T03/T04)  
**Revisión requerida:** conformidad y calidad
