# Evidence Report — P06-T05

**Estado:** in_review  
**Implementación:** (commit on `implementation/P06-T05-performance`)  
**Entorno:** local · Python 3.13.12 · pytest 9.1.1 · ruff 0.16.5 · mypy 2.3.1 · CPython · macOS-26.5.2-arm64  
**Dataset hash:** `674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b`  
**Baseline hash:** `174a5de5dc2075c733f8a027508388b666635ca8f7d17044fd0473fb9f9e4b8b` (`evals/baselines/mvp-performance.json`)  
**Compare outcome:** PASS (`metric` match vs approved summarized baseline)  
**Gate decision:** PASS (eval + resilience + performance; production SLO deferred until EXT-007)

## Resultado

Escenarios controlados `mvp-baseline` (FAQ concurrente A/B, workflow largo, corpus creciente, queue burst, upstream lento) con presupuestos de **diseño** por span. El schema exige latency-by-span, throughput, errors y queue age. El agregador de calidad consume el reporte T02 (`EvalReport`) y evidencia T04 (`ResilienceEvidence`) sin reescribir runner/scorers ni la security suite. CI programado/release en `.github/workflows/quality-evidence.yml`.

No se declara capacidad/SLO de producción (EXT-007 pendiente). Outputs voluminosos quedan en `build/` (gitignored); el baseline versionado es un resumen.

## Rojo observado

```text
uv run --extra quality pytest tests/performance/test_models.py -v
ImportError: No module named 'ia_mcp.performance'
```

Faltaban `latency_by_span` / `errors` / `queue_age` y el paquete. Después: `ModuleNotFoundError: ia_mcp.performance.cli` y `ia_mcp.evals.quality_report`.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/performance tests/evals/unit -v
61 passed
exit 0

uv run python -m ia_mcp.performance run --scenario mvp-baseline
gate: PASS (pass)
baseline_compare: PASS (pass)
dataset hash: 674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b
throughput: 34.59 ops/s · errors: 0 · queue age p95: 2190.0ms
exit 0

uv run python -m ia_mcp.evals run --suite smoke --provider fake
gate: PASS (pass)
cases: 6 FAQ smoke
exit 0

uv run --extra quality pytest -m resilience tests/resilience -q
32 passed, 12 deselected
exit 0

uv run python -m ia_mcp.evals.quality_report gate \
  --eval build/evals.json --resilience build/resilience.json \
  --performance build/performance.json \
  --baseline evals/baselines/mvp-performance.json
gate: PASS (pass)
production_slo: deferred until EXT-007
exit 0

uv run --extra quality ruff check src/ia_mcp/performance src/ia_mcp/evals/quality_report.py tests/performance tests/evals/unit/test_quality_report.py
All checks passed!

uv run --extra quality mypy src/ia_mcp/performance src/ia_mcp/evals
Success: no issues found in 14 source files
```

## Criterios

| ID | Estado |
|---|---|
| AC-P06-008 | satisfied — carga reporta latency por span, throughput, errors y queue age; vacío/ausente se rechaza |
| AC-P06-009 | satisfied — commit, dataset hash, model/config y environment en performance y quality reports |

T02/T04 se consumen: eval JSON del runner aceptado y resilience suite (`tests/resilience`) vía `ResilienceEvidence`. No se reescribió `redaction.py` ni `run_query.py`.

## Decisiones y desviaciones

Presupuestos por span son de diseño (RNF-007 / testing-strategy), no SLO de negocio. `production_slo_declared` es siempre `false` hasta EXT-007.

Las latencias del escenario son determinísticas (costos controlados por span + corpus/upstream), no wall-clock, para que el compare contra baseline no sea flaky.

## Riesgos residuales

EXT-007 sigue abierto: este gate no afirma capacidad productiva. ReDoS residual en `redaction.py` (T03) no se tocó. P07-T02 `run_query` no se editó.

## Handoff

**Próxima tarea desbloqueada:** ninguna de forma aislada. P08-T03 sigue esperando P08-T02 y P07-T04 además de P06-T05.
