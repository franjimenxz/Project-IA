# Evidence Report — P06-T04

**Estado:** in_review  
**Entorno:** local · Python 3.13.12 · pytest 9.1.1 · ruff 0.16.5  
**Rojo observado:** `ModuleNotFoundError: No module named 'tests.fixtures.faults'` al colectar `tests/resilience/test_dependencies.py` y `test_scheduler_recovery.py` (hooks before/after ausentes).

## Resultado

Fault injection determinista (sin red ni sleep real) sobre DB, Redis, LLM, KB, MCP, channel y handoff, en boundaries **before** y **after** del side effect. Consume el puerto `FaultPlan` existente para errores MCP; los wrappers viven en `tests/fixtures/faults.py`.

| Dependencia | Before | After | Side-effect count | Outcome |
|---|---|---|---|---|
| MCP create | 0 appointments | 1 appointment | 0 / 1 | `manual_review_required`; replay no duplica |
| DB `cas_advance` | 0 transitions | 1 transition persistida | 0 / 1 | retry o replay idempotente |
| Redis cache | durable ok, cache vacío | cache escrito luego falla | 0 / 1 | estado durable intacto; replay sin duplicar |
| LLM | generate no completa | generate ocurrió luego falla | 0 / 1 | `insufficient`; no inventa hechos |
| KB search | search no completa | search ocurrió luego falla | 0 / 1 | `insufficient`; LLM no se llama |
| Channel send | 0 deliveries | 1 delivery | 0 / 1 | retry → `dispatched` único |
| Handoff | 0 cases | 1 transfer + persist | 0 / 1 | recovery una vez; replay por business key |

Workflow crash/reload SQL (P04) ahora corre bajo marker `resilience`. Scheduler recovery: restart de worker, retry before/after channel, outbox replay sin reenvío.

## Archivos

- `tests/fixtures/faults.py`
- `tests/resilience/test_dependencies.py`
- `tests/resilience/test_scheduler_recovery.py`
- `tests/resilience/test_workflow_recovery.py`

No se editaron `tests/e2e/test_mvp_journeys.py` ni `tests/security/*`. No se cambiaron contratos de producción; hooks sólo en fakes de test.

## Comandos

```text
uv run --extra quality pytest -m resilience tests/resilience -v
40 passed in 1.12s
exit 0

uv run --extra quality ruff check tests/fixtures/faults.py tests/resilience/test_dependencies.py tests/resilience/test_scheduler_recovery.py tests/resilience/test_workflow_recovery.py
All checks passed!
exit 0
```

## Criterios

| Criterio | Prueba | Resultado |
|---|---|---|
| AC-P06-007 | `tests/resilience/test_dependencies.py` (before/after por dependencia) + recovery workflow/scheduler | pass |

Retry: channel (before/after) y DB before. Manual review: MCP timeout before/after create. Recovery: DB after, Redis after, workflow SQL restart, scheduler lock expiry / outbox replay. Sin doble mutación en todos los casos after.

## Controles estáticos y seguridad

Ruff en archivos de la tarea: limpio. TenantContext explícito en todos los boundaries. `FaultPlan` existente produce `ToolError` tipado sin secretos. Redis modelado como caché no autoritativa (TDD sistema §17).

## Decisiones y desviaciones

ninguna sobre contratos. Redis aún no existe en producción; el fake de test demuestra pérdida de caché/coordinación sin borrar estado durable.

## Riesgos residuales

`CreateAppointmentDefinition._await_settled` usa `asyncio.sleep(0.01)` en producción; esta suite no transita ese path. Fault `after` de MCP no está en `FaultPlan` sticky de `src/` (el plan original falla antes de mutar); el wrapper de test cubre after sin cambiar el fake de P03-T04.

## Handoff

**Próxima tarea desbloqueada:** P06-T05  
**Revisión requerida:** conformidad y calidad
