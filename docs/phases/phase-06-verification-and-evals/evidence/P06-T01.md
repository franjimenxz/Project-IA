# Evidencia P06-T01 — Dataset de evals

**Estado:** in_review  
**Implementación:** `9d0b1ba`  
**Dataset hash:** `0ad0756001a221d6ccc56950b5ddebc5160d145180b2af761c7e5f80ba03bf30`  
**Revisión:** pendiente (conformidad + calidad)

## Resultado

Schemas estrictos `EvalCase` / `DatasetValidationReport` y dataset sintético versionado `evals/datasets/mvp.jsonl` (15 casos). `validate_dataset(Path)` comprueba IDs únicos, allowed/forbidden disjoint, referencias de tenant/skill/tool/source existentes, cobertura UC-01–10 y hash SHA-256. CLI: `python -m ia_mcp.evals validate evals/datasets/mvp.jsonl`. Sin completions, prompts ni datos reales.

## Distribución

| Dimensión | Conteo |
|---|---|
| UC-01–UC-03, UC-05–UC-07, UC-09–UC-10 | 1 cada uno |
| UC-04 | 2 (create + timeout) |
| UC-08 | 5 (FAQ A/B, insuficiencia, injection, tool forbidden) |
| tenant_a | 13 |
| tenant_b | 2 |
| adversarial | insufficient 1, injection 1, tool-forbidden 1, timeout 1, handoff 1 |

## Evidencia TDD

- RED: `pytest tests/evals/unit/test_dataset.py -v` → `ModuleNotFoundError: No module named 'ia_mcp.evals'` (validator ausente).
- GREEN: duplicate ID y allowed/forbidden overlap rechazados; MVP valida schema, IDs, refs y hash.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/evals/unit/test_dataset.py -v
8 passed

uv run python -m ia_mcp.evals validate evals/datasets/mvp.jsonl
valid=true cases=15 hash=0ad0756001a221d6ccc56950b5ddebc5160d145180b2af761c7e5f80ba03bf30
use_cases=UC-01:1,UC-02:1,UC-03:1,UC-04:2,UC-05:1,UC-06:1,UC-07:1,UC-08:5,UC-09:1,UC-10:1
tenants=tenant_a:13,tenant_b:2
adversarial=handoff:1,injection:1,insufficient:1,timeout:1,tool-forbidden:1

uv run --extra quality mypy src/ia_mcp/evals
Success: no issues found in 4 source files

uv run --extra quality ruff check src/ia_mcp/evals tests/evals/unit/test_dataset.py
All checks passed!
```

## Criterios

| ID | Estado |
|---|---|
| AC-P06-001 | satisfied — JSONL valida schema, IDs únicos, corpus sintético |
| AC-P06-003 | satisfied (dataset) — tenant/sources/tools prohibidos quedan en el caso para scoring determinístico; disjoint validado |
| AC-P06-004 | partial — casos críticos (forbidden tool, isolation, timeout) están en el dataset; el gate de promedio vs crítico es P06-T02 |

## Revisión y riesgos

- Critical/Important abiertos: ninguno.
- Residual: source IDs del dataset son fixtures estables (`kb-a-hours`, …), no IDs de ingestión runtime (`{document_id}:vN:p:pos`).
- Próxima tarea desbloqueada: P06-T02.
