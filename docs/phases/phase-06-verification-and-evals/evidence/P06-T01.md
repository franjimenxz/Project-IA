# Evidencia P06-T01 — Dataset de evals

**Estado:** accepted  
**Implementación:** `9d0b1ba`, `ecda2b9`, `31fd3dd` merged `3da861c`  
**Dataset hash:** `674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b`  
**Revisión:** conformidad PASS; calidad PASS (PR #26)

## Resultado

Schemas estrictos `EvalCase` / `DatasetValidationReport` y dataset sintético versionado `evals/datasets/mvp.jsonl` (17 casos). `validate_dataset(Path)` comprueba IDs únicos, allowed/forbidden disjoint, referencias contra catálogo independiente `evals/fixtures/known_sources.json`, cobertura semántica UC-01–10, multi-turn y hash SHA-256. CLI: `python -m ia_mcp.evals validate evals/datasets/mvp.jsonl`. Sin completions, prompts ni datos reales.

Correcciones de conformidad:

- UC-10 es scheduler (dispatch / skip / dedupe) con `appointments.get`; confirmación de paciente queda en UC-07.
- Conversaciones sintéticas incluyen multi-turn (`user`/`assistant`); el validador falla si todos los casos son single-turn.
- Source IDs se validan contra allowlist externa; el catálogo incluye IDs no usados en el JSONL (`kb-a-parking`, `kb-b-parking`).

## Distribución

| Dimensión | Conteo |
|---|---|
| UC-01–UC-03, UC-05–UC-07, UC-09 | 1 cada uno |
| UC-04 | 2 (create multi-turn + timeout) |
| UC-08 | 5 (FAQ A/B, insuficiencia, injection, tool forbidden) |
| UC-10 | 3 (dispatch, skip, dedupe) |
| tenant_a | 15 |
| tenant_b | 2 |
| adversarial | insufficient 1, injection 1, tool-forbidden 1, timeout 1, handoff 1 |

## Evidencia TDD

- RED inicial: `ModuleNotFoundError: No module named 'ia_mcp.evals'`.
- RED conformidad: assistant role rechazado; dataset sin multi-turn; UC-10 con `appointments.confirm`; `source_catalog` ausente.
- GREEN: 15 passed; MVP valida schema, IDs, catálogo, multi-turn, UC-10 scheduler y hash.

## Verificación

```text
uv run python --version
Python 3.13.12

uv run --extra quality pytest tests/evals/unit/test_dataset.py -v
15 passed

uv run python -m ia_mcp.evals validate evals/datasets/mvp.jsonl
valid=true cases=17 hash=674c1ad36d680415f8fb8bb30e6f4a576e8d278042abc98f9ff058150c10599b
use_cases=UC-01:1,UC-02:1,UC-03:1,UC-04:2,UC-05:1,UC-06:1,UC-07:1,UC-08:5,UC-09:1,UC-10:3
tenants=tenant_a:15,tenant_b:2
adversarial=handoff:1,injection:1,insufficient:1,timeout:1,tool-forbidden:1

uv run --extra quality mypy src/ia_mcp/evals
Success: no issues found in 4 source files

uv run --extra quality ruff check src/ia_mcp/evals tests/evals/unit/test_dataset.py
All checks passed!
```

## Criterios

| ID | Estado |
|---|---|
| AC-P06-001 | satisfied — JSONL valida schema, IDs únicos, corpus sintético |
| AC-P06-003 | partial — el dataset declara tenant/sources/tools prohibidos y disjoint; el scoring determinístico es P06-T02 |
| AC-P06-004 | partial — casos críticos (forbidden tool, isolation, timeout) están en el dataset; el gate de promedio vs crítico es P06-T02 |

## Revisión y riesgos

- Two-stage review: conformidad PASS; calidad PASS. Critical/Important abiertos: ninguno.
- Residual: source IDs son fixtures de catálogo (`kb-a-hours`, …), no IDs de ingestión runtime (`{document_id}:vN:p:pos`).
- Próxima tarea desbloqueada: P06-T02.
