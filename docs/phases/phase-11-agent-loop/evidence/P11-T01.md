# Evidencia P11-T01 — ADR-006 y documentación de fase

**Tarea:** P11-T01 — ADR-006 y documentación de fase  
**Estado:** accepted  
**Implementación:** `310a553`  
**Rama:** documentación en `main` (commit `310a553`)  
**Revisión:** two-stage PASS at `310a553`

## Resultado

ADR-006 y la Fase 11 documentan el hueco entre el turno FAQ y la maquinaria MCP, y deciden el cierre: tipo de decisión separado (`ToolCallProposal` / `LLMTurnDecision`) sin tocar `AnswerKind`, loop acotado en el harness, superficie invocable fail-closed como cuarto término de ADR-005, realimentación saneada y spans del turno. Las mutaciones siguen fuera del loop (ADR-003 intacto). Sin código de producción.

## Revisión 1 — Conformidad PASS

Alcance documental respetado: ADR-006, README/TDD/plan/criterios/test-plan y briefs T01–T04. `src/**` y `tests/**` no se tocaron. ADR-003 y ADR-005 sin enmienda. Criterio AC-P11-001 cubierto. Cada afirmación de estado actual en el ADR cita archivo y línea verificables contra `main` (`9380843` / harness, models, composition, executor).

## Revisión 2 — Calidad PASS

La decisión es aditiva y con defaults; no inventa API, vendor, auth ni campos institucionales. Dependencias abiertas (`EXT-007`, contrato de config, sink durable) quedan explícitas y no se resuelven de contrabando. Firmas de T02 (`LLMTurnDecision`, `tool_results`, `tool_calls`) coinciden entre ADR, TDD y brief. Mutaciones siguen en workflow; el puente turno→workflow se difiere a un ADR propio.

## Verificación del coordinador

```text
python3 scripts/check_docs.py --all docs
# exit 0

python3 scripts/check_traceability.py
# exit 0
```

`pytest tests/docs` no se re-ejecutó aquí: el entorno del coordinador no tenía el extra `quality` instalado. Los validadores documentales del brief pasaron.

## Residuales

- El loop no se implementa en T01; T02–T04 siguen serializadas.
- Campo de config para superficie de turno / límites por tenant: no existe; T03 escala si lo necesita.
- Sink durable de tools sin workflow: owner P07; T04 no lo cierra.
- Presupuesto `agent.run` 800 ms vs loop: `EXT-007` / P06-T05.

## Próximo paso

P11-T02 `ready`. P11-T03–T04 `blocked`. P05-T01–T04 siguen `blocked` por EXT.
