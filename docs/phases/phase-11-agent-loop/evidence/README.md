# Evidencia — Fase 11

Guardar:

- salida de tests de contrato de decisión y observaciones;
- loop acotado, superficie invocable y errores tipados;
- suite negativa multi-tenant del loop;
- spans `agent.run` / `llm.generate` y `run_id` en `ToolAuditEvent`;
- regresión FAQ y evals sin cambio de expectativa.

No commitear credenciales, tokens ni payloads con PII.
