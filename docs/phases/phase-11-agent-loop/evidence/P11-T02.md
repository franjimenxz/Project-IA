# Evidencia P11-T02 — Contrato de decisión y realimentación

**Tarea:** P11-T02 — Contrato de decisión y realimentación  
**Estado:** accepted  
**Implementación:** `3fbebfc`  
**Rama:** `cursor/p11-t02-decision-contract-9100`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/72  
**Revisión:** two-stage PASS at `3fbebfc`

## Resultado

`LLMPort.generate` devuelve `LLMTurnDecision` (`LLMDecision | ToolCallProposal`) sin cambiar `AnswerKind`. `LLMRequest.tool_results` y `AgentTurnResult.tool_calls` se agregan con default `()`. `observation_from` sanea el `ToolResult` y nunca copia `upstream_reference`. `observe_turn` usa `tool_calls` ejecutadas cuando hay y `tool_names` anunciadas cuando no.

## Revisión 1 — Conformidad PASS

Alcance respetado. `harness.py`, `faq.py`, `outbox.py` y `evals/models.py` no se tocaron. `AnswerKind` intacto. Criterios AC-P11-002, AC-P11-003, AC-P11-005 cubiertos por tests. Desviación aceptada: `observation_from` vive en `observations.py` para no importar contratos desde `models.py`.

## Revisión 2 — Calidad PASS

Unión aditiva; `FakeLLM` sigue válido. Sanitización reusa `sanitize_summary`. Fixtures sin secretos. `exhaustive: Never` de evals intacto. Residual esperado: `mypy src` falla en `harness.py:165` (`AnswerPolicy.apply` espera `LLMDecision`); owner P11-T03.

## Verificación del implementador

```text
# RED: ImportError LLMTurnDecision / ExecutedToolCall
# GREEN: 11 + 58 evals + 16 harness/compiler; ruff clean; mypy evals/runner.py clean
```

## Próximo paso

P11-T03 `in_progress`. P11-T04 `blocked`. P05 sigue `blocked` por EXT.
