Tarea: P11-T02
Estado: in_review
Resultado: `LLMPort.generate` devuelve `LLMTurnDecision` (`LLMDecision | ToolCallProposal`) sin cambiar `AnswerKind`. `LLMRequest.tool_results` y `AgentTurnResult.tool_calls` se agregan con default `()`. `observation_from` sanea el `ToolResult` con `sanitize_summary` y nunca copia `ToolError.upstream_reference`. `observe_turn` usa `tool_calls` ejecutadas cuando hay y `tool_names` anunciadas cuando no.
Commit: 3fbebfc
Archivos: src/ia_mcp/agent_runtime/models.py, src/ia_mcp/agent_runtime/ports.py, src/ia_mcp/agent_runtime/observations.py, src/ia_mcp/evals/runner.py, tests/unit/agent/test_models.py, tests/unit/agent/test_tool_observations.py
Pruebas: Python 3.13.15, `uv run --extra quality`

RED (esperado, antes de implementar):

```text
uv run --extra quality pytest tests/unit/agent/test_models.py tests/unit/agent/test_tool_observations.py -v
# ERROR collecting test_models.py
# ImportError: cannot import name 'LLMTurnDecision' from 'ia_mcp.agent_runtime.models'
# ERROR collecting test_tool_observations.py
# ImportError: cannot import name 'ExecutedToolCall' from 'ia_mcp.agent_runtime.models'
```

La falla es la esperada: el puerto y los modelos no podían expresar `ToolCallProposal`, `tool_results` ni `observation_from`. Sin esos tipos no se puede construir una observación de error que excluya `upstream_reference`.

GREEN (mismos tests, 11 passed):

```text
uv run --extra quality pytest tests/unit/agent/test_models.py tests/unit/agent/test_tool_observations.py -v
# 11 passed

uv run --extra quality pytest tests/evals -v
# 58 passed

uv run --extra quality pytest tests/unit/agent/test_harness.py tests/unit/agent/test_context_compiler.py -v
# 16 passed

uv run --extra quality ruff check src tests
# All checks passed

uv run --extra quality mypy src/ia_mcp/evals/runner.py
# Success: no issues found in 1 source file
```

`mypy src/ia_mcp/agent_runtime src/ia_mcp/evals` reporta un error único en `harness.py:165` (owner P11-T03): `AnswerPolicy.apply` sigue esperando `LLMDecision | None` y el puerto ahora devuelve la unión. No se editó `harness.py`. Los chequeos `exhaustive: Never` de `_outcome_from_kind` / `_skill_from_kind` no se tocaron.

Criterios: AC-P11-002, AC-P11-003, AC-P11-005
Decisiones: ADR-006 only
Desviaciones: `observation_from` vive en `src/ia_mcp/agent_runtime/observations.py` para no importar contratos ni `sanitize_summary` desde `models.py`.
Riesgos residuales: `mypy --strict` sobre `src/ia_mcp/agent_runtime` queda rojo en `harness.py:165` hasta que P11-T03 discrimine `ToolCallProposal` antes de `AnswerPolicy.apply`. El loop, el executor y la superficie invocable no se implementaron.
Próxima tarea desbloqueada: P11-T03
