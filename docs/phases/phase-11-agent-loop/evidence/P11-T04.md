Tarea: P11-T04
Estado: in_review
Resultado: El loop no cruza el boundary de tenant. `agent.run` es el span raíz del turno (desde `runs.start` de FAQ hasta finish). Cada `self._llm.generate` emite `llm.generate` hijo. Un turno que ejecuta una tool de lectura produce `agent.run` + 2 `llm.generate` + 1 `tool.execute` + 1 `mcp.resolve`. Todo `ToolAuditEvent` del turno lleva `run_id` del turno y `tenant_id` del `TenantContext`. Tenant A no ejecuta una tool descubierta sólo en el MCP de B (`forbidden`, sin transporte, `allowed=False`). Turnos concurrentes usan executors distintos. Ampliar la allowlist de A no cambia la superficie de B. `tenant_isolation_violation` aborta sin realimentar, run `failed`, auditoría `allowed=False` con ese código.
Commit: c2d12a0a28a972a9b9ab8678689db47d89c005da
Archivos: src/ia_mcp/agent_runtime/harness.py, tests/security/test_tool_loop_isolation.py, tests/security/test_observability.py, docs/phases/phase-11-agent-loop/evidence/P11-T04.md
Pruebas: Python 3.13.15, venv con `.[quality]`

RED (esperado, spans del turno ausentes):

```text
pytest tests/security/test_tool_loop_isolation.py \
  tests/security/test_observability.py::test_tool_loop_turn_emits_agent_run_generate_execute_and_resolve -v
# isolation: 5 passed
# span: FAILED
# AssertionError: assert 0 == 1
# names = ['mcp.resolve', 'tool.execute']
```

La falla es la esperada: `tool.execute` y `mcp.resolve` ya salen del executor (T03 pasa `run_id` a `execute`); `agent.run` y `llm.generate` no se emitían. `ToolAuditEvent.run_id == turn.run_id` ya pasaba (T03).

GREEN:

```text
pytest tests/security/test_tool_loop_isolation.py \
  tests/security/test_observability.py::test_tool_loop_turn_emits_agent_run_generate_execute_and_resolve \
  tests/security/test_observability.py::test_spans_omit_content_payload_and_secrets \
  tests/security/test_observability.py::test_metric_labels_drop_high_cardinality_ids \
  tests/security/test_observability.py::test_unauthenticated_request_ignores_foreign_correlation \
  tests/security/test_observability.py::test_signed_simulated_message_ignores_forged_correlation_header -v
# 10 passed

pytest tests/unit/agent tests/unit/mcp/test_executor.py -v
# 65 passed

ruff check src tests
# All checks passed

mypy src/ia_mcp/agent_runtime
# Success: no issues found in 7 source files
```

Los 7 casos `@pytest.mark.integration` de `test_observability.py` no se ejecutaron aquí: no hay PostgreSQL en 127.0.0.1:5432. No se editaron.

Criterios: AC-P11-010, AC-P11-011
Decisiones: ADR-006 §8, §9; ADR-002; semconv sin claves nuevas
Desviaciones: ninguna. No se tocó `semconv.py`, adapters de observabilidad, `executor.py`, workflows ni el tablero.
Riesgos residuales: las ejecuciones de tool del loop **no** aparecen en `RunInvestigation.tools`. Esa vista se arma desde filas de `workflow_transition` cuyo `event_type` empieza con `tool.` (`src/ia_mcp/observability/adapters/sqlalchemy_run_query.py`). Una tool del loop no tiene workflow. El sink durable es dependencia abierta de P07; no se implementó. FAQ `allowed_tools` sigue vacío: en producción el loop queda inerte salvo factory de prueba. `run_id` viaja como atributo de span, no como label de métrica.
Próxima tarea desbloqueada: ninguna (P11-T04 cierra el wave de aislamiento/trazas del loop)
