# Evidencia P11-T04 — Aislamiento del loop y observabilidad

**Tarea:** P11-T04 — Aislamiento, auditoría y trazas del loop  
**Estado:** accepted  
**Implementación:** `c2d12a0`  
**Rama:** `cursor/p11-t04-isolation-observability-8f76`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/76  
**Revisión:** two-stage PASS at `c2d12a0`  
**Agente:** `bc-e670623f-3505-5b92-864c-88a6ed868f76`

## Resultado

El loop no cruza el boundary de tenant: A no ejecuta un tool descubierto solo para B; turnos concurrentes no cruzan `tool_results` ni `run_id`; un allowlist de A no cambia el conjunto de B. `tenant_isolation_violation` aborta el turno y no rellena `tool_results`. El turno abre `agent.run`; cada `generate` abre `llm.generate`. Atributos solo de `ALLOWED_SPAN_ATTRIBUTES`. `run_id` no entra en labels de métricas. `semconv.py` intacto.

## Revisión 1 — Conformidad PASS

Solo `harness.py` y tests del brief. Criterios de aislamiento y observabilidad cubiertos. `start_span(SPAN_AGENT_RUN)` envuelve el loop después del `runs.start` de FAQ. Cada `generate` usa `SPAN_LLM_GENERATE`. Atributos: `run_id`, `tenant_id`, `config_version`, `skill`. Sin índice de iteración.

## Revisión 2 — Calidad PASS

Un archivo de producción. Fixtures sin secretos. CI `platform-quality`, `documentation-quality` y `database-quality` verdes en PR #76. Residual: tools del loop no entran en `RunInvestigation.tools` (filas `workflow_transition` `tool.*`; sink durable P07). `run_id` es atributo de span, no label de métrica.

## Verificación

```bash
PYTHONPATH=src python3 -m pytest tests/security/test_tool_loop_isolation.py tests/security/test_observability.py tests/unit/agent/test_harness_loop.py tests/unit/agent/test_harness_loop_errors.py -q
python3 scripts/check_docs.py --all docs
python3 scripts/check_traceability.py
```

## Próximo paso

Fase 11 T01–T04 accepted. P05 sigue `blocked` por EXT.
