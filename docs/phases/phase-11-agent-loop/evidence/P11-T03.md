Tarea: P11-T03
Estado: in_review
Resultado: `AgentHarness` ejecuta un loop acotado. Una `ToolCallProposal` invocable pasa por `ToolExecutor.execute` (factory por turno, skill `"faq"`) y provoca una segunda `llm.generate` con `tool_results` saneados. Una `LLMDecision` terminal provoca exactamente una generate. `executors=None` o `max_tool_iterations=0` restauran el turno de una sola llamada. El término `turn` de `invocable_en_turno` es mecánico: canónica sin `_require_idempotency_key` (`search`/`get`); `create`/`cancel`/`reschedule`/`confirm` y toda tool no canónica no declarada son `forbidden` sin execute. Presupuesto y deadline cierran `insufficient` sin respuesta parcial. Dos `forbidden` cierran `handoff`. `tenant_isolation_violation` aborta sin realimentar. Sin retry en el harness. `AnswerPolicy.apply` sólo ve `LLMDecision`.
Commit: d9fdde340b7de571906deb88fbe2348ca3c8167a
Archivos: src/ia_mcp/agent_runtime/harness.py, src/ia_mcp/api/composition.py, tests/unit/agent/test_harness_loop.py, tests/unit/agent/test_harness_loop_errors.py, tests/unit/agent/test_turn_tool_surface.py
Pruebas: Python 3.13.15, venv con `.[quality]`

RED (esperado, constructor sin loop):

```text
pytest tests/unit/agent/test_harness_loop.py tests/unit/agent/test_harness_loop_errors.py tests/unit/agent/test_turn_tool_surface.py -v
# 26 failed
# TypeError: AgentHarness.__init__() got an unexpected keyword argument 'executors'
```

La falla es la esperada: el harness no aceptaba factory ni límites, no ejecutaba `ToolCallProposal` y `AnswerPolicy.apply` recibía la unión.

GREEN:

```text
pytest tests/unit/agent/test_harness_loop.py tests/unit/agent/test_harness_loop_errors.py tests/unit/agent/test_turn_tool_surface.py -v
# 26 passed

pytest tests/unit/agent/test_harness.py tests/unit/mcp/test_executor.py -v
# 19 passed

pytest tests/unit/api/test_composition.py tests/unit/handoff/test_harness_guard.py tests/unit/agent tests/evals/unit -v
# 132 passed

ruff check src tests
# All checks passed

mypy src/ia_mcp/agent_runtime src/ia_mcp/api
# Success: no issues found in 14 source files
```

Criterios: AC-P11-004, AC-P11-006, AC-P11-007, AC-P11-008, AC-P11-009, AC-P11-012
Decisiones: ADR-006 §2–§5, §7; ADR-003 y ADR-005 sin enmienda
Desviaciones: el parámetro `executors` se tipa como `ToolExecutorFactory` (Protocol en `harness.py` que coincide con `TenantToolExecutors.for_tenant`) para no importar composition desde el runtime. `clock` opcional con default `time.monotonic` para el deadline determinista. No se agregó campo de superficie en `TenantConfig`.
Riesgos residuales: `FAQSkill.allowed_tools` sigue vacío, así que `TenantToolExecutors.for_tenant(..., "faq")` en producción tiene intersección de skill vacía y el loop queda inerte salvo factory de prueba. Tools no canónicas siguen fail-closed hasta un campo de superficie de turno (coordinación). Auditoría crítica de `tenant_isolation_violation` usa el `audit_hook` del executor si existe; no se inventó un sink. Spans `agent.run` / `llm.generate` son P11-T04. El workflow engine no se invoca desde el turno.
Próxima tarea desbloqueada: P11-T04
