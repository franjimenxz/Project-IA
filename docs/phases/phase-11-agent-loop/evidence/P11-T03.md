# Evidencia P11-T03 — Loop acotado en el harness

**Tarea:** P11-T03 — Loop acotado en el harness  
**Estado:** accepted  
**Implementación:** `d9fdde3`  
**Rama:** `cursor/p11-t03-harness-loop-25b5`  
**PR:** https://github.com/franjimenxz/Project-IA/pull/74  
**Revisión:** two-stage PASS at `d9fdde3`

## Resultado

`AgentHarness` ejecuta un loop acotado: `ToolCallProposal` invocable pasa por un executor construido por turno; `LLMDecision` terminal hace exactamente un `generate`. Superficie fail-closed (search/get sí; mutaciones y nombres no declarados, no). Presupuesto, deadline, dos `forbidden` → handoff, `tenant_isolation_violation` aborta sin realimentar. `mypy` de `harness.py:165` queda verde.

## Revisión 1 — Conformidad PASS

Solo `harness.py`, `composition.py` y tests del brief. `executor.py`, workflows, `TenantConfig` y `semconv.py` intactos. Factory tipada como Protocol (sin importar FastAPI). Criterios AC-P11-004, 006, 007, 008, 009, 012.

## Revisión 2 — Calidad PASS

Executor por turno desde `TenantContext`. `run_id` del turno llega a `execute`. Reloj inyectable. FAQ existente sigue verde con `executors=None`. CI `platform-quality` verde. Residual: skill FAQ sin allowlist de lectura (loop inerte en production); sin campo de superficie en config.

## Próximo paso

P11-T04 `in_progress`. P05 sigue `blocked` por EXT.
